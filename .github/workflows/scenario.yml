# Tier 3 — make test-scenario
# Builds all twin binaries then runs the scenario suite (-race, -tags=scenario, 10m).
# Covers test/scenario/... and internal/daemon/... scenario build-tag files.
# Non-merge-blocking: no required status check configured in branch protection.
name: Scenario

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:
  schedule:
    # Nightly at 04:00 UTC so failures surface before the next workday.
    - cron: "0 4 * * *"

jobs:
  scenario:
    name: scenario (Tier 3)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      # TEMPORARY (flywheel, per operator directive): continue-on-error so failed runs
      # report SUCCESS and stop emailing the operator. This job currently fails to BUILD on
      # Linux (hk-x342d, monotonic_linux.go) before any scenario runs. REMOVE once green;
      # hk-jzepv owns removal + the ubuntu-vs-macos runner decision.
      - name: make test-scenario
        run: make test-scenario
        continue-on-error: true
