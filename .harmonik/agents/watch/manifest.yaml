type: watch
cardinality: { min: 0, max: 1 }
harness: claude

identity:
  soul: soul.md
  parent_intent: captain

context:
  - { ref: boot,              as: skill,       presence: injected }
  - { ref: operating.md,      as: instruction, presence: injected }
  - { ref: agent-comms,       as: skill,       presence: injected }
  - { ref: harmonik-dispatch, as: skill,       presence: retrieved }
  - { ref: beads-cli,         as: skill,       presence: retrieved }

triggers:
  - { id: bus, source: event, enabled: true }

handoff:
  channel: private

keeper:
  thresholds: default

lifecycle:
  self_restart: true

tools_dir: null

markers:
  never_emits:
    - queue_submit:main
