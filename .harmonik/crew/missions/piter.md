schema_version: 1
crew_name: piter
queue: piter-q
epic_id: hk-q3ovr
captain_name: captain
goal: |
  LANE #3 (operator priority order) — CODEX-AS-CREW (MR1), epic hk-q3ovr — RESEARCH PHASE.

  ⛔ OPTION B IS PERMANENTLY KILLED (operator hard-no, 2026-07-11). The daemon-re-invokes
  `codex exec resume` per-wake model is DEAD — do NOT resurface, re-propose, or reason from it
  again. The spike (hk-fijwi) is CLOSED. Discard the Option-B framing entirely.

  YOUR WORK NOW: a FULL kerf work item deeply researching the resident `codex app-server` path.
  - Open a kerf work (codename e.g. `codex-app-server`) and run the full cycle:
    problem-space -> research -> design. Write artifacts to the bench path `kerf new`/`kerf show`
    prints (NOT repo-local .kerf). No implementation this phase.
  - Research question: how does `codex app-server` actually work; what would running a harmonik
    crew ORCHESTRATOR on a resident long-context Codex session involve; and can a resident
    long-context model RETIRE the keeper/compaction machinery Claude sessions depend on?
  - Ground findings in the real app-server surface (proto/driver, session lifecycle, context
    handling), NOT the retired Option-B design.
  - hk-l63b9 (route crew-start through harness selection) stays OPEN-PARKED until the design
    names the integration path. Do NOT dispatch it yet.

  Model posture: Opus (research/design lane — judgment required).
  Progress feed per crew contract: comms --topic status AND br comments — on milestone +
  <=15min idle-drain, + boot/drain bookends. Arm comms recv --follow.
  PING captain at the design-review gate. Escalate genuine blockers to captain.
