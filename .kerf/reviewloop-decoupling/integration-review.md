# Integration review gate

## Round 1 — REQUEST_CHANGES

The reviewer found an unremoved template, split caller/handler Minted identity ownership, and a missing
`last_iteration_head_sha` entry in Handler's implicit review-loop context registry.

## Round 2 — REQUEST_CHANGES

After those repairs, Handler's `LaunchSpec` schema still restricted continuity identity to resume.

## Round 3 — APPROVE

The schema now covers every Minted Claude launch and Captured omission until native observation. The
integration report, context registry, and identity ownership are coherent.

