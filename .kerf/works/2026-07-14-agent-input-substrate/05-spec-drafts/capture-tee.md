# 05 spec-draft index — component `capture-tee` → target spec files

> The `capture-tee` research/design component (C4 live capture tee, apptap production wiring)
> produces NO spec file of its own. Its normative text lives in the target-spec draft below.

- **`agent-input.md`** (NEW, prefix AIS) — AIS-013 pipes-only best-effort capture tee (apptap's first
  production consumer), AIS-014 consumer-side persistence/redaction/corpus-layout/retention,
  AIS-INV-002 capture-never-aborts-the-run.
