# 05 spec-draft index — component `harness` → (DoD, no normative spec draft)

> The `harness` research/design component (C5 replay harness + L0–L3 taxonomy + fault injection) is
> DoD / acceptance-and-test material, NOT normative spec prose (per the M2-5 task card). It therefore
> produces NO spec-draft file. Its only normative footprint is the conformance obligation of
> **AIS-INV-001**, drafted in `agent-input.md` §10 (Conformance) and §5 (Invariants).
>
> The harness design (package layout, fault matrix, N=10 oracle, coverage floor, SC6 enforcement
> gates) is captured in `04-design/harness-acceptance-design.md` and broken into implementation
> task **T9** in `07-tasks.md`. The two validation beads (hk-1cjy5 scenario, hk-1r5jt exploratory)
> are the close-gate.
