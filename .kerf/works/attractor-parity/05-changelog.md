# Spec-draft changelog — `attractor-parity`

> Pass 5 (`spec-draft`) changelog. One entry per target spec file per component, with status (new/modified), what changed, and the motivating change design (`04-design/{component}-design.md`).
>
> **This file is appended by component, not rewritten.** Each agent owns its own `## <component>` section header; do not edit another component's section. The integration pass (pass 6) reconciles overlapping target-file edits (notably the shared `workflow-graph.md §4 WG-002` row and `§10 WG-031` reserved set, which multiple components amend).

---

## Independent components (D, E) — per-node-model, goal-template-params

Drafts: `05-spec-drafts/per-node-model.md`, `05-spec-drafts/goal-template-params.md`.

| Target spec file | Status | What changed | Motivating design |
|---|---|---|---|
| `specs/workflow-graph.md` | modified | §4 WG-002 `agentic` row gains optional attrs `model`, `effort`; new requirement **WG-042** (per-node model/effort: shape-validated `model`, closed-enum `effort` as ingest-strict, `agentic`-only); new INFORMATIVE requirement **WG-043** (`class`/`model_stylesheet` accepted permissively, never dispatched on, port-to-`model=` note); §10 WG-031 reserved set gains `model`, `effort` (NOT `class`/`model_stylesheet`); §16.1 diff row; OQ-2 marked RESOLVED. | `04-design/per-node-model-design.md` (component D) |
| `specs/execution-model.md` | modified | §4.3 EM-012b "resolve once" paragraph reworded to bind the tier-1..4 walk and frame the sealed pair as the run-level *default*; new sub-clause EM-012b-NODE (per-node `model`/`effort` as static-graph tier-0 layered at dispatch, not a second resolution walk, replay-safe; cites `[workflow-graph.md §4 WG-042]`); §6.1 Node RECORD gains optional `model : String\|None` and `effort : EffortLevel\|None`; §6.4 additive schema-evolution note. | `04-design/per-node-model-design.md` (component D) |
| `specs/workflow-graph.md` | modified | New requirement **WG-044** (graph-level `goal` attr → typed `Graph.Goal`, threaded into agentic briefs via run-level ExtraContext, composes with `prompt` §4 WG-040 / bead body); **WG-045** (template-param substitution `__[A-Z][A-Z0-9_]*__` over raw source pre-parse, `--param` map, residual-token = launch error, trust-boundary: operator-supplied/trusted, un-sanitized, non-recursive); **WG-046** (ordering invariant read→substitute→parse→validate→dispatch); §10 WG-031 reserved set gains `goal`; §16.1 diff rows. | `04-design/goal-template-params-design.md` (component E) |
| `specs/execution-model.md` | modified | §6.1 Workflow RECORD gains optional `goal : String\|None`; §6.1 Run RECORD gains optional `template_params : Map<String,String>\|None` (sealed at claim, applied once before parse, replay-stable); §6.4 additive schema-evolution note; §7.5 `dot` dispatcher launch surface amended (accept `--param` map → seal → substitute-before-parse → residual check → parse/validate → surface `goal` via ExtraContext). | `04-design/goal-template-params-design.md` (component E) |

All four entries are additive / minor / N-1-readable (`schema_version` stays `1`). The one wording change to a load-bearing invariant (EM-012b "resolve once") is a clarification, not a relaxation — flagged for pass-6 integration. **Pass-6 ID reassignment (RESOLVED):** D's provisional `WG-039` / informal `WG-002a` → **WG-042** / **WG-043**; E's provisional `WG-039` / `WG-040` / `WG-041` → **WG-044** / **WG-045** / **WG-046** (see `06-integration.md` §1.2 for the full OLD→NEW map).

**B↔E coupling flagged for pass-6 integration:** component E's `goal` (run-level, via ExtraContext channel) and component B's `prompt` (node-level, replacing taskBody) both write the `agent-task.md` brief; they occupy distinct channels and MUST compose, not double-inject. See `05-spec-drafts/goal-template-params.md` §C.

<!-- Foundation components (A inline-prompt / B / C non-committing / tool-node) append their section below this line. Do not edit the D/E section above. -->

## Foundation components (A tool-node, B inline-prompt, C non-committing)

Drafts: `05-spec-drafts/tool-node.md`, `05-spec-drafts/inline-prompt.md`, `05-spec-drafts/non-committing.md`.

| Target spec file | Status | What changed | Motivating design |
|---|---|---|---|
| `specs/workflow-graph.md` | modified | §4 WG-002 `non-agentic` row gains optional attrs `tool_command`, `timeout`; new requirement WG-039 (tool-command attr semantics: `tool_command`+`timeout` make a tool node, `handler_ref="shell"` pinned, default timeout 300s, trust-boundary normative — literal shell string from trusted `.dot` author, no sandbox/escape at v1); §9 WG-024 gains tool-node attr validation (`handler_ref=shell` warn-at-v1, `timeout` non-neg integer); §10 WG-031 reserved set gains `tool_command`, `timeout`. | `04-design/tool-node-design.md` (component A) |
| `specs/handler-contract.md` | modified | New requirement HC-063 (built-in `shell` handler: `/bin/sh -c` in worktree, in-process exception from §4.2 wire-protocol / §4.9 ready / §4.6 silent-hang obligations, exit-state→Outcome map 0→SUCCESS / non-zero→FAIL+`deterministic` / timeout→FAIL+`transient` / signal→FAIL+`canceled`, default axis_tags `io-determinism=non-deterministic`/`replay-safety=unsafe`); §4.2 HC-005 in-process reconciliation note ("built-in handlers MAY be in-process; only `shell` at v1"); §4.2a HC-058 `non-agentic` (tool-style) row reconciled with `shell` handler classification. | `04-design/tool-node-design.md` (component A) |
| `specs/execution-model.md` | modified | **§7.5.4 EM-058 `non-agentic` TABLE ROW amended** (the keystone code↔spec reconciliation): row now splits dispatch on `tool_command` — tool node (`tool_command`+`handler_ref=shell`) runs in-process `shell` handler; start/terminal/pass-through node (no `tool_command`) synthesizes SUCCESS without a handler (reconciles `dot_cascade.go:198-203`); other non-agentic nodes invoke `handler_ref` as before; EM-058 sub-note added; §7.5.3 EM-057 item 7 `non-agentic` sub-bullet gains `handler_ref="shell"` clause (preserves mandatory-handler_ref). | `04-design/tool-node-design.md` (component A) |
| `specs/workflow-graph.md` | modified | §4 WG-002 `agentic` row gains optional attr `prompt`; new requirement WG-040 (inline `prompt`: REPLACES bead-derived task body for implementer-class nodes, bead Title/ID stay in header, reviewer-class `prompt` accepted-but-inert at v1, input-only — Outcome/cascade untouched); §10 WG-031 reserved set gains `prompt`. | `04-design/inline-prompt-design.md` (component B) |
| `specs/handler-contract.md` | modified | §4.2 HC-006a `agent-task.md` content row note: Body MAY be sourced from node `prompt` (overriding bead body) for `dot`-mode implementer nodes; no LaunchSpec required-field / wire-protocol / Outcome change. | `04-design/inline-prompt-design.md` (component B) |
| `specs/workflow-graph.md` | modified | §4 WG-002 `agentic` row gains optional attr `non_committing`; new requirement WG-041 (`non_committing` boolean: implementer-class clean exit ⇒ SUCCESS without HEAD-advance, default false, clean-exit⇒SUCCESS v1 floor — no work-product/marker parsing, `auto_status` reserved/not-accepted, authoring rule: pair with validating tool node, reviewer/non-agentic/gate inert); §10 WG-031 reserved set gains `non_committing`. | `04-design/non-committing-design.md` (component C) |
| `specs/execution-model.md` | modified | §4.3 EM-015d gains scoping clause: implementer commit obligation is **review-loop-scoped** and does NOT apply to `workflow_mode=dot` (v69 review-loop untouched); §7.5 EM-058 non-committing dispatch sub-note (dot-mode implementer outcome-derivation gated on `non_committing`; unresolvable HEAD still a daemon error both modes). | `04-design/non-committing-design.md` (component C) |
| `specs/handler-contract.md` | modified | §4.2a (near HC-058) clarifying note: `dot`-mode agentic node MAY emit SUCCESS without a commit when `non_committing`; already legal per EM-005, no new HC requirement. | `04-design/non-committing-design.md` (component C) |

All foundation entries are additive / minor / N-1-readable (`schema_version` stays `1`). Outcome envelope (EM-005/EM-005a/EM-005c) and the edge cascade (EM-041) are unchanged. The one wording touch to a load-bearing invariant — EM-015d's implementer-MUST-commit — is a **scoping clarification** (it states the obligation is review-loop-scoped and carves out `dot` mode), NOT a relaxation of the review-loop behavior; the v69 review-loop path does not regress. Flagged VERIFY for pass-6 integration (I-C-em015d-scope).

### Cross-component reconciliation the integration pass (pass 6) MUST handle

- **WG ID collision — RESOLVED in pass 6.** Components D/E claimed `WG-039` (model/effort), `WG-040` (goal), `WG-041` (substitution/ordering); foundation A/B/C provisionally used the same `WG-039`/`WG-040`/`WG-041` for tool-command/prompt/non_committing. Pass 6 (`06-integration.md` §1.2) allocated the next free sequential block after the live max (WG-038): **A tool-command → WG-039, B prompt → WG-040, C non_committing → WG-041, D model/effort → WG-042, D class/stylesheet (informative) → WG-043, E goal → WG-044, E substitution → WG-045, E ordering → WG-046.** The foundation set (A/B/C) keeps WG-039/040/041 (literal values unchanged, now unambiguous); only D/E shift up. All cross-references fixed in `SPEC.md` (HC-063 / EM-057 / EM-058 cite WG-039; `non-committing.md` pairing rule cites WG-039; EM-012b-NODE / §6.1 Node-RECORD cite WG-042; E's internal refs cite WG-044/045/046). HC-063 stands (HC-062 was the prior max; no HC collision).
- **Shared WG-002 `agentic` row.** Components B (`prompt`), C (`non_committing`), and D (`model`, `effort`) all add optional attrs to the SAME `agentic` row. Pass 6 produces ONE merged row: optional attrs = `prompt`, `non_committing`, `model`, `effort`, `idempotency_class`, `axis_tags`, `skills_ref`, `freedom_profile_ref`, `budget_ref`, `hook_ref`, `guard_ref`.
- **Shared WG-002 `non-agentic` row.** Component A adds `tool_command`, `timeout`. No other component touches this row.
- **Shared WG-031 reserved set.** ONE merged sentence adding `tool_command`, `timeout` (A), `prompt` (B), `non_committing` (C), `model`, `effort` (D), `goal` (E).
- **B↔C shared function.** Both thread through `dispatchDotAgenticNode` in non-overlapping regions (B pre-launch brief build; C post-launch outcome derivation) — implementation-sequencing note, no spec-text conflict.
- **B↔E brief-channel composition.** Component E's run-level `goal` (ExtraContext channel) and component B's node-level `prompt` (replaces taskBody) both write the agent brief; they occupy distinct channels and MUST compose, not double-inject. Already flagged by the D/E section above; foundation B's WG-040 input-only posture is consistent.
- **Tool-node observability events (I-A3) — RESOLVED in pass 6.** Decision: REUSE the existing node-lifecycle surface (`node_dispatch_requested` per [event-model.md §8.1.11], emitted by the live `dot` cascade before every node, plus the standard Outcome / run-terminal events). NO `tool_command_completed` event is added at v1 — event-model.md §8 explicitly excludes per-tool-call lifecycle events, and the in-process `shell` handler has no NDJSON stream. No `event-model.md` amendment is required by this work. Rationale: `06-integration.md` §6.
