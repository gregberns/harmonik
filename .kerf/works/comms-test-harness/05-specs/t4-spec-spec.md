# Change Spec — T4 (spec/doc reconciliation)

Deliverable: an executable spec (assertion pinning the CURRENT semantics, not a behavior change) + a doc
note in `.claude/skills/agent-comms/SKILL.md`, for both B1 and B2. Do NOT change runtime behavior. Surface
both as explicit design questions to captain (`--topic gate` or `status`) for operator relay, and wait for
verdict before any follow-up behavior-change bead is created. Verification: the pinning assertion passes
against current code as-is; the doc note is reviewed for accuracy against the T1/T2 evidence beads.
