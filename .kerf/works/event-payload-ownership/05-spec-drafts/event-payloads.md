# Event Payloads — Draft Mapping

This component maps to the full target draft
`05-spec-drafts/event-model.md`. That file is the complete updated text for
`specs/event-model.md`.

This file is a Kerf component artifact. It is not a target specification file
and it is not normative text for `specs/event-payloads.md`.

The target draft changes the one-owner rule, `run_started` version 2,
`handler_capabilities`, `liveness_halt`, and `stale_open_bead_detected` as
defined in `04-design/event-payloads-design.md`.
