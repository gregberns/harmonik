# Event model — no normative spec amendment

`specs/event-model.md` is unchanged.

The status writer preserves event ownership and persist-before-emit order. It
does not add or alter an event record.
