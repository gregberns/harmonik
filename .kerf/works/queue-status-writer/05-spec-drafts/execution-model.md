# Execution model — no normative spec amendment

`specs/execution-model.md` is unchanged.

The status writer preserves current run-to-item outcomes. The deferred-recovery
store call is an Alpha integration handoff. It adds no execution requirement.
