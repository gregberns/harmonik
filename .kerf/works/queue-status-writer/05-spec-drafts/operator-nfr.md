# Operator NFR — no normative spec amendment

`specs/operator-nfr.md` is unchanged.

The status writer uses the existing pause and resume requirements. It changes
only the implementation path that makes those changes durable.
