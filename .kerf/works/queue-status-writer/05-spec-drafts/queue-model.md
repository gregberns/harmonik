# Queue model — no normative spec amendment

`specs/queue-model.md` is unchanged.

The queue status writer implements the existing state-machine and transaction
requirements. `04-design/queue-model-design.md` records the implementation
boundary. It adds no state, wire field, durable record, or requirement.
