# Agent Index

This is the master map for this project's knowledge base.
Add links to docs, specs, and guides here as the project grows.

See [STATUS.md](STATUS.md) for current project state and [AGENTS.md](AGENTS.md) for
agent instructions. The active work list is the bead ledger, not a file. How an agent
finds work in it belongs in that agent's mission — but note that `br ready` stops at
20 rows and does not say so, so a short listing is never evidence of a short backlog;
`--limit 0` defeats it.
