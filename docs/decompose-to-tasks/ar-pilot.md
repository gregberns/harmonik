# AR Pilot — Decompose-to-Tasks Worked Example

`pilot-version: 0.2.2` — re-drafted 2026-04-27 against `specs/architecture.md` v0.3.1 (status `reviewed`) and discipline v0.5 (after r1 review; see `docs/reviews/2026-04-27-ar-pilot-r1/synthesis.md` for the lane-by-lane decisions that drove discipline v0.5 and the pilot-lane fixes). v0.2.1 patch dropped the invented edge `ar-011 → ar-029` per F-pilot-AR-9. v0.2.2 patch dropped two "per X" supporting-cite edges (`ar-029 → ar-011` and `ar-029 → ar-018`) surfaced during the load attempt: a 3-cycle `ar-011 → ar-030 → ar-029 → ar-011` was real in v0.2.1 spec inline cites because all three "→" steps were treated as hard `blocks` deps. The "per §4.5.AR-018 and §4.3.AR-011" cite in AR-029's body attaches to a stand-alone assertion ("No 'verifier' subsystem exists") that is independently testable without AR-011/AR-018 — so it is reclassified as informational per a new pattern (F-pilot-AR-10) the v0.6 discipline patch will codify alongside the v0.5 r2 findings. Second instance of the decompose-to-tasks discipline applied to a normative spec; first non-BI spec. AR is declaration-heavy and root-of-the-corpus (`depends-on: []`), so the bead inventory skews toward first-class declarations with zero outbound cross-spec edges and zero multi-step protocols.

> Companion: [discipline.md](discipline.md) v0.5 (the rules), [bi-pilot.md](bi-pilot.md) v0.1.3 (the structural template). Read the discipline first; this file applies it. Compare to BI for shape — BI is a behavioural / protocol-heavy spec, AR is a meta-rule / declaration-heavy spec; the contrast is informative for the discipline author.

## 1. Spec under decomposition

- **Spec:** `specs/architecture.md` (AR), v0.3.1, status `reviewed`. (v0.3.1 is the current front-matter version; v0.3.1 was a citation-drift cleanup pass over v0.3.0's invariant promotions and §4.0 envelope-slot rework.)
- **Counts:**
  - **49 active §4 first-class req beads** (50 active §4 normative requirements `AR-001` … `AR-053` minus AR-035 collapsed to a `notes:` line on `ar-026` per discipline §2.1a; three retired IDs do not mint beads). The §2 row count is therefore 49.
  - **2 retired §4 IDs present as rows:** `AR-037` (promoted to AR-INV-007 at v0.3.0), `AR-046` (promoted to AR-INV-008 at v0.3.0). Plus **`AR-008` retired at v0.2.0** (no row in spec body; anti-pattern list moved to AR-007 EXAMPLE block). All three IDs are never reused. Per discipline §2.11(b), retired IDs do NOT mint beads; spec-parent description enumerates them.
  - **4 active §5 invariants** (`AR-INV-001`, `AR-INV-003`, `AR-INV-007`, `AR-INV-008`). **4 retired §5 IDs:** `AR-INV-002`, `AR-INV-004`, `AR-INV-005`, `AR-INV-006` (all retired at v0.2.0 as duplicates of §4 reqs per template selection test; never reused).
  - **1 §6 schema construct:** the agent-type identifier regex declared in §6.1 (`agent_type := ^[a-z][a-z0-9-]{1,62}$` plus the four reserved identifiers). This is AR's only data-shape declaration; §6 of the spec explicitly defers all other type schemas to downstream owning specs. Per discipline §2.6 (v0.5), the bead is tagged `kind:primitive-shape`.
  - **0 §8 error-taxonomy entries.** AR has no §8 section. (The spec has §10 Conformance immediately after §9.)
  - **0 multi-step protocol requirements** in §4 — no §4 requirement contains a numbered step list. AR is meta-rule / declaration-heavy.
  - **7 retired IDs total** across §4 + §5 (3 reqs: AR-008, AR-037, AR-046; plus 4 invariants: AR-INV-002, AR-INV-004, AR-INV-005, AR-INV-006); the spec-parent description enumerates all seven.
- **Front-matter `depends-on`:** `[]` (empty). AR is the root of the foundation corpus (per §9.1). Consequently this pilot emits **zero outbound cross-spec edges** (per discipline §3.1 + §3.2 — every cross-spec edge target must appear in `depends-on`; with `depends-on: []` the universe is empty).
- **§9.3 co-references:** all 7 entries point at `docs/foundation/components.md §N` (a bootstrap stub, NOT a finalized sibling spec). Per discipline §2.7 third class, co-references generate no bead-level edges regardless. Per §A.1 envelope exemplar (appendix, non-normative) the spec uses placeholder `[event-model.md §6.3]` / `[handler-contract.md §N]` / `[execution-model.md §6.1]` / etc. — appendix cites do not generate edges per discipline §3.1.
- **AR is `spec-category: foundation-cross-cutting`.** Per AR-052 self-classification, AR is exempt from envelope declaration (it would otherwise require a §4.a Subsystem envelope per AR-053). No AR-ENV-NNN beads exist.

**Spec parent bead (mnemonic `ar`).** Loaded as:

    br create --silent --type epic --title "Architecture spec — implementation" \
              --labels "spec:architecture,kind:spec-parent" \
              --description "Implements specs/architecture.md v0.3.1 (49 first-class req beads from 50 active §4 reqs after AR-035 collapse per discipline §2.1a, 4 invariants, 1 schema, 0 error taxonomy). Retired (never reused): AR-008 [v0.2.0], AR-037 → AR-INV-007 [v0.3.0], AR-046 → AR-INV-008 [v0.3.0], AR-INV-002, AR-INV-004, AR-INV-005, AR-INV-006 [all v0.2.0]."
    # then: br update <id> --status draft

Beads assigns the actual ID (e.g., `hk-<base36-suffix>`) per discipline §2.10 mnem→assigned-ID rule and §2.12 corpus-prefix rule. Children are minted with `--parent <ar-assigned-id>`.

**What AR is about (2-3 sentence summary).** AR encodes harmonik's architectural meta-contracts: the four-axis determinism classification, the ZFC mechanism/cognition test, the search+verifier+traces required-triple, the subsystem envelope and Go-package MVH realization, the foundation-amendment protocol with parallel-amendment serialization, the agent-type abstraction and verification-name disambiguation, the role taxonomy, the centralized-controller invariant with its acknowledged tradeoff, and the three-artifact separation forbidding "feature" as a fourth compositional primitive. Every other foundation and subsystem spec is expected to depend on AR; AR itself depends on nothing and emits no normative inline cross-spec edges.

---

## 2. Per-requirement task table

Columns: bead-id (mnemonic) · title (≤80 chars) · description (cites the source requirement ID and summarises what to build in 1-2 sentences) · tags · blocks edges (citing bead → prerequisite, loaded as `br dep add <citing> <prerequisite> -t blocks`) · notes.

Tags abbreviated: `mech` = `tag:mechanism`; `cog` = `tag:cognition`. Axes shown only when off-baseline. Every bead also implicitly carries `spec:architecture` and is `parent-child` of `ar` (omitted from table for terseness). All edges in this section are intra-spec; cross-spec edges are §5 (and §5 is empty for AR — `depends-on: []`).

| bead-id | title | description | tags | blocks edges (citing bead → prerequisite) | notes |
|---|---|---|---|---|---|
| `ar-052` | Spec-category front-matter (runtime-subsystem vs foundation-cross-cutting) | Per AR-052: every spec under `specs/` MUST declare in front matter `spec-category ∈ {runtime-subsystem, foundation-cross-cutting}`. Front-matter presence is lint-enforced; category assignment is reviewer-enforced. | mech, `req:AR-052` | `ar-016` | First requirement under §4.0 (placed before AR-001 numerically out-of-order; it gates AR-013 / AR-053). v0.2.0: added `ar-016` edge — AR-052 inline-cites "Go-package inside the daemon binary per §4.5.AR-016" (per r1 synthesis 2.4 non-bidirectional half + discipline §3.1 step 5 term-use). The reverse `ar-052 → ar-053` cite is informational plumbing per discipline §2.7 F13 slot-rule heuristic (no edge emitted). |
| `ar-053` | Envelope declaration section slot (§4.a) with reserved `<PREFIX>-ENV-NNN` ID range | Per AR-053: every `runtime-subsystem` spec MUST carry `§4.a Subsystem envelope` as the first §4 subsection; envelope IDs use the reserved `<PREFIX>-ENV-NNN` range to avoid consuming topical ID space; eight envelope elements per AR-013; reference template in §A.1. | mech, `req:AR-053` | `ar-052`, `ar-013` | AR-053 is the slot rule; AR-013 (envelope content) and AR-052 (subsystem categorization that names the slot obligation) are the content. Per discipline §2.7 F13 slot-rule heuristic: `ar-053 → {ar-013, ar-052}` are the normative deps (slot points at what fills it); the reverse cites `ar-013 → ar-053` and `ar-052 → ar-053` are informational plumbing — no edge emitted. Resolution recorded as F-pilot-AR-8 (closed). |
| `ar-001` | Four-axis classification applies to every cross-subsystem surface | Per AR-001: every type/interface/evaluation point in a cross-subsystem contract MUST be classifiable on `(llm-freedom, io-determinism, replay-safety, idempotency)`; `Axes:` line required on any requirement deviating from baseline or involving LLM/external I/O/state mutation/non-idempotent effects per template §4.N+1. | mech, `req:AR-001` | (none) | Foundation rule; many AR reqs ultimately ground here. |
| `ar-002` | Baseline axis values are the default | Per AR-002: baseline tuple is `llm-freedom=none; io-determinism=deterministic; replay-safety=safe; idempotency=idempotent`. A baseline-matching requirement MAY omit `Axes:`; deviation requires the full four-axis tuple. | mech, `req:AR-002` | `ar-001` | |
| `ar-003` | Profile for skeleton vs organ operations (daemon vs handler) | Per AR-003: skeleton operations (daemon routing/dispatch/checkpoint emission/edge selection/validator execution) MUST profile as `llm-freedom=none` and `io-determinism ∈ {deterministic, best-effort}`; organ operations (agent cognition in handler subprocess) MUST profile as `llm-freedom ∈ {bounded, unbounded}`. Reviewer-enforced per §10.2 (lint checks tag grammar, not daemon/handler-boundary semantics). | mech, `req:AR-003` | `ar-001`, `ar-002` | Constrains where cognition can live; AR-INV-001 sensors this. |
| `ar-004` | Classification applies to every subsystem-exported type | Per AR-004: every type a subsystem exports across its envelope (including types transitively referenced by any cross-subsystem event payload) MUST carry the four-axis tuple and the mechanism/cognition tag. Marking a type "internal" to escape tagging when transitively cited is forbidden. | mech, `req:AR-004` | `ar-001`, `ar-005`, `ar-013` | Closes the loophole on transitively-cited types; INFORMATIVE block on polymorphic/sum-type tagging is non-edge-generating per discipline §3.1. v0.2.0: added `ar-013` term-use edge — AR-004 references "envelope" defined by AR-013 (per discipline §3.1 step 5). |
| `ar-005` | Every evaluation point is tagged mechanism or cognition | Per AR-005: every normative requirement in every spec MUST carry `Tags:` with exactly one of `mechanism` or `cognition`; mutual exclusion. Requirements describing both surfaces MUST split. | mech, `req:AR-005` | (none) | Root of the ZFC test; sensor for AR-INV-001 routes through here. |
| `ar-006` | Mechanism-tagged surface definition (deterministic-only) | Per AR-006: a mechanism-tagged point MUST NOT invoke an LLM. Permitted: I/O, schema/type checks, deterministic policy enforcement, typed state transitions, typed error handling. Semantic-judgment patterns (keyword-match, heuristic fallback trees, regex of unstructured output, hardcoded quality scoring) are ZFC violations. | mech, `req:AR-006` | `ar-005` | EXAMPLE block (anti-pattern list from retired AR-008) is non-edge-generating per discipline §3.1. |
| `ar-007` | Cognition-tagged delegation path obligation | Per AR-007: a cognition-tagged point MUST delegate to an LLM under a named delegation path naming role (per §4.8), model class/handler, and input shape. Gestures without these three are insufficient. Reviewer-enforced (not lint-enforced); spec-authoring obligation. | mech, `req:AR-007` | `ar-005`, `ar-032` | Delegation path requires AR-032 role names; INFORMATIVE block clarifies AR-007 is itself mechanism-tagged (it is a meta-rule about spec shape). |
| `ar-009` | All three required mechanisms exist in every deployment | Per AR-009: every deployment MUST include search (AR-010), verification (AR-011), and traces (AR-012). Removing any of the three from a deployment is non-conforming. | mech, `req:AR-009` | `ar-010`, `ar-011`, `ar-012` | Umbrella for the required-triple invariant AR-INV-003. |
| `ar-010` | Search mechanism at the foundation level | Per AR-010: backtracking representable via four transition kinds (`local-patchback`, `architectural-rollback`, `policy-rollback`, `context-restore`); candidate generation expressible as a node type (`agentic` with policy-driven freedom profile); freedom profiles per state declarable via YAML policy reference. The three surfaces are owned by execution-model and control-points (cited at `docs/foundation/components.md §N` bootstrap stubs; see §8 OQ-pilot-AR-2 below). | mech, `req:AR-010` | (none) | Foundation pin only — implementation surfaces live in EM/CP. No outbound edge per discipline §3.1 (the cite is to a stub document, not a spec, and AR's `depends-on: []` forbids cross-spec edges anyway). |
| `ar-011` | Verifier is a role-function on a workflow node, not a subsystem | Per AR-011: verification MUST be realized as a role-function (`agentic` with `actor_role=Reviewer` or `non-agentic` deterministic checker); NOT a node-type enum value; NOT a subsystem; no subsystem named "verifier". | mech, `req:AR-011` | `ar-030`, `ar-032` | v0.2.1: dropped invented edge `ar-029` per F-pilot-AR-9 (AR-011's body inline-cites only `§4.7.AR-030`; the AR-029 reference was a v0.1.0 citation-projection error). Cross-references AR-030 (verification-result shape). `ar-032` is a term-use edge per discipline §3.1 step 5 (uses "Reviewer" role). |
| `ar-012` | Traces are durable AlphaGo decision records distinct from events | Per AR-012: every durable state transition MUST produce a trace with 11 fields (prior state, actor role, candidate actions, chosen action, policy version, parameter vector, evidence, outcome, verifier metrics, next state, confidence). Distinct from events; events MAY project but MUST NOT replace. | mech, `req:AR-012` | (none) | OQ-AR-006 tracks trace-shape uniformity under deterministic dispatch; not bead-loaded per discipline §2.8. |
| `ar-013` | Subsystem envelope declaration (8 elements) | Per AR-013: every `runtime-subsystem` spec MUST declare envelope in §4.a slot (per AR-053): (a) events produced, (b) events consumed, (c) types introduced (with §4.1 + §4.2 tags), (d) handlers implemented, (e) state owned, (f) control points provided, (g) NFRs inherited/overridden, (h) boundary classification per operation. `foundation-cross-cutting` specs are exempt per AR-052. Reference template in §A.1. | mech, `req:AR-013` | `ar-001`, `ar-005`, `ar-052` | The §A.1 exemplar contains placeholder cross-spec cites that do NOT generate edges (appendix per discipline §3.1). |
| `ar-014` | Subsystem obligations (forbidden actions) | Per AR-014: a subsystem MUST NOT invent shared vocabulary not grounded in foundation; MUST NOT violate mechanism/cognition boundary by performing cognition in framework code; MUST NOT skip NFR compliance by omitting NFR declaration; MUST NOT export-while-marking-internal to escape AR-004. | mech, `req:AR-014` | `ar-013`, `ar-004`, `ar-005` | Negation-form rule; reviewer-enforced. v0.2.0: added `ar-005` term-use edge — AR-014 references "mechanism/cognition boundary" classifier defined by AR-005 (per discipline §3.1 step 5). |
| `ar-015` | Add-a-subsystem procedure (no foundation revision required) | Per AR-015: a new subsystem MUST be added by writing a new subsystem spec referencing only foundation specs and existing siblings. Foundation revision MUST NOT be required. If a gap exists, capture as foundation amendment per AR-020 first. | mech, `req:AR-015` | `ar-013`, `ar-020` | |
| `ar-016` | Subsystem is a Go package inside the daemon for MVH | Per AR-016: for MVH, a subsystem MUST be realized as a Go package inside the daemon binary; the event bus is in-process; consumer taxonomy classes (in-process-synchronous, in-process-asynchronous, fan-out-observer) all live in the same binary. | mech, `req:AR-016` | (none) | Ground for AR-017's enumeration of out-of-process actors. |
| `ar-017` | Enumerated out-of-process actors (closed list) | Per AR-017: only out-of-process actors admitted in MVH are (a) agent-handler subprocesses, (b) orchestrator-agent sessions, (c) `br` CLI invocations. None is a subsystem. Adding a fourth class MUST proceed via AR-020 amendment. | mech, `req:AR-017` | `ar-016`, `ar-020`, `ar-013` | Closed-set rule; reviewer-enforced enumeration. v0.2.0: added `ar-013` term-use edge — AR-017 says "None of (a), (b), (c) is a subsystem" using the "subsystem" concept defined by AR-013's envelope rule (per discipline §3.1 step 5). |
| `ar-018` | Reconciliation is not a subsystem (workflow-library entry) | Per AR-018: reconciliation MUST NOT be a subsystem; it is a workflow-library entry (named DOT workflows + YAML policies) that runs on the same primitives. Declares no envelope; introduces no shared types. | mech, `req:AR-018` | `ar-013` | |
| `ar-019` | Post-MVH process geometry is out of foundation scope | Per AR-019: if harmonik later splits to multi-process, envelope semantics MUST remain unchanged (every Go package declaring an envelope is a subsystem regardless of host process). Process-geometry change MUST NOT require foundation revision. | mech, `req:AR-019` | `ar-016`, `ar-013` | v0.2.0: added `ar-013` term-use edge — AR-019 references "envelope semantics" defined by AR-013 (per discipline §3.1 step 5). |
| `ar-020` | Amendment-proposal procedure (paragraph + 2 reviewer personas) | Per AR-020: a downstream gap-discovery MUST produce an amendment proposal of ≥1 paragraph; reviewed by ≥2 foundation-review personas (architect + critic minimum); on accept, foundation revised + every dependent subsystem spec re-reviewed; on reject, downstream adapts. | mech, `req:AR-020` | (none) | |
| `ar-021` | Amendment authority — material-change cognition delegation | Per AR-021: amendments are product-level decisions. Orchestrator agent proposes; reviewer personas critique; user approves material changes. Material-change determination is a **cognition-tagged Reviewer step**: role=Reviewer (architect persona per build-practices.md); model class=agentic (LLM-backed reviewer subagent invoked through handler-contract); input shape=amendment proposal + diff of proposed change against prior foundation baseline; output=structured `material \| non-material` verdict with written rationale. | cog, `axis:llm-freedom-bounded`, `axis:io-determinism-best-effort`, `req:AR-021` | `ar-020`, `ar-032` | **Only cognition-tagged AR req.** Description quotes the full delegation path per discipline §2.8 cognition rule. AR-032 dependency for "Reviewer" role naming. |
| `ar-022` | Foundation versioning and conformance citation | Per AR-022: foundation MUST be versioned; every subsystem spec MUST cite the foundation version it conforms to in its front matter or §9.1 Depends-on; foundation-version bump MUST appear in revision-history row introducing the change. | mech, `req:AR-022` | (none) | |
| `ar-023` | Parallel-amendment serialization with `touches:` overlap detection | Per AR-023: every amendment proposal MUST declare `touches:` list of affected sections + requirement IDs; overlapping proposals MUST serialize (rebase later onto earlier merged, or reject-with-reason if premise removed); non-overlapping MAY merge in any order. Overlap = non-empty lexical intersection of `touches:` lists. Detection mechanism (script vs kerf check vs reviewer scan) deferred to OQ-AR-003; reviewer-enforced until tool lands. | mech, `req:AR-023` | `ar-020` | OQ-AR-003 NOT a bead per discipline §2.8. |
| `ar-024` | Agent-type is a handler-contract conformance class | Per AR-024: foundation defines `agent type` as a handler-contract conformance class. Every handler instance MUST be an instance of the same abstraction comprising (a) Go `Handler` interface, (b) wire protocol, (c) event-emission contract, (d) secrets-delivery contract — all owned by handler-contract / event-model. | mech, `req:AR-024` | (none) | |
| `ar-025` | Agent-type identifier shape (regex + reserved IDs + registration) | Per AR-025: `agent_type` identifier MUST match `^[a-z][a-z0-9-]{1,62}$`; reserved MVH IDs `{claude-code, pi, claude-twin, pi-twin}`; registration is (a) subsystem spec declaring identifier in envelope + (b) handler-contract conformance claim. Daemon-scoped; no namespace prefix at MVH. | mech, `req:AR-025` | `ar-schema.agent-type-identifier`, `ar-024` | Schema-bead edge for the regex. OQ-AR-002 tracks reverse-DNS post-MVH discipline; not a bead. |
| `ar-026` | Agent-type orthogonal to role | Per AR-026: agent type MUST NOT be conflated with role (§4.8). Role = function assignment (Planner/Builder/Reviewer/etc.); agent type = process assignment (Claude Code / Pi / twin process). Same agent type MAY fill different roles across runs and vice-versa. | mech, `req:AR-026`, `req:AR-035` | `ar-024`, `ar-032` | AR-035 (role-orthogonality cross-reference) collapsed here per discipline §2.1a — see source spec §4.8.AR-035. The §4.8 cross-reference is structurally guaranteed by the spec's existing prose; no separate impl work. |
| `ar-027` | Cross-subsystem agent-type reference points (4 surfaces, byte-equal) | Per AR-027: four surfaces MUST reference `agent_type` with the AR-025 shape byte-for-byte: (i) YAML policies, (ii) DOT node attributes, (iii) `LaunchSpec.agent_type`, (iv) event payloads naming an agent. Canonical field name `agent_type` across all four. Mismatch is corpus-lint-detectable per §10.2. | mech, `req:AR-027` | `ar-025` | INFORMATIVE block on AR-MIG-001 (handler_type → agent_type rename) is non-edge-generating; tracked in §12 revision history (also non-edge-generating per discipline §3.1). |
| `ar-028` | Adding a new agent type does not require foundation revision | Per AR-028: adding a new agent type MUST be an exercise of the subsystem envelope procedure (AR-013, AR-016). New handler package writes a subsystem spec citing handler-contract + declaring envelope. | mech, `req:AR-028` | `ar-013`, `ar-024`, `ar-016` | v0.2.0: added `ar-016` edge — AR-028's body inline-cites the subsystem envelope procedure as "(§4.4, §4.5)" → AR-013 (already present) plus AR-016 (Go-package realization in §4.5) per discipline §3.1 step 5 + intra-spec inline cite. |
| `ar-029` | Verification is a role-function, not a node-type enum value | Per AR-029: verification MUST be a role-function of a workflow node, NOT a `node-type` enum member. Canonical enum (per execution-model) is `{agentic, non-agentic, gate, control-point, sub-workflow}`; no `verification-node` value admitted. `agentic` + `actor_role=Reviewer` is verification-capable (cognition-tagged); `non-agentic` deterministic-checker is verification-capable (mechanism-tagged). The term `verification-node` in this and other specs refers to so-configured nodes, not an enum value. | mech, `req:AR-029` | `ar-032` | v0.2.0: dropped invented `ar-016` edge (per r1 review finding 2.1); added `ar-032` term-use edge (uses "Reviewer" role per discipline §3.1 step 5). v0.2.2: dropped `ar-011` and `ar-018` "per X" supporting-cite edges per F-pilot-AR-10 (the "No 'verifier' subsystem exists per §4.5.AR-018 and §4.3.AR-011" cite is informational — AR-029's enum claim is independently testable; AR-011/AR-018 are cited only as consistency back-references for a stand-alone assertion). Resolved the 3-cycle `ar-011 → ar-030 → ar-029 → ar-011` surfaced at load. |
| `ar-030` | Verification-result shape (no RETRY admissible) | Per AR-030: a `verification-result` MUST be the outcome of a `verification-node` (per AR-029) and conform to the outcome shape declared in execution-model with `status ∈ {SUCCESS, FAIL, PARTIAL_SUCCESS}`. The `RETRY` outcome status is NOT admissible for a verification-result (a verification node returning RETRY MUST fail-hard — classification is a category error). Mechanism-tagged verification nodes populate `evidence`; cognition-tagged populate `notes`. Hyphenated form is canonical. | mech, `req:AR-030` | `ar-029`, `ar-005` | v0.2.0: added `ar-005` term-use edge — AR-030 distinguishes mechanism-tagged vs cognition-tagged verification nodes using AR-005's classifier (per discipline §3.1 step 5). |
| `ar-031` | Quality-gate is a gate that reads a verification-result | Per AR-031: a `quality-gate` MUST be a gate (per control-points) whose evaluator references a prior verification-result. Quality-gates are gates that read verification outcomes; they are NOT themselves verification. Three hyphenated forms (`verification-node`, `verification-result`, `quality-gate`) are distinct and MUST NOT be used interchangeably. | mech, `req:AR-031` | `ar-030`, `ar-029` | v0.2.0: added `ar-029` term-use edge — AR-031 references "verification-node" defined by AR-029 (per discipline §3.1 step 5; `ar-030` already present for "verification-result"). |
| `ar-032` | Seven roles named (canonical role vocabulary) | Per AR-032: foundation names seven roles drawn from the AlphaGo north-star: Planner, Researcher, Builder, Reviewer, Verifier, Scheduler, Governor. These names MUST be canonical across all specs. A subsystem spec inventing alternatives for an already-covered function fails review. | mech, `req:AR-032` | (none) | |
| `ar-033` | MVH-required vs declared-but-deferred role split | Per AR-033: Planner/Builder/Reviewer MUST be implemented at MVH (sufficient to run the minimum self-build cycle). Researcher/Verifier/Scheduler/Governor are declared-but-deferred (named in foundation so subsystems do not invent alternatives, but not required at MVH). Each deferred role MUST be activatable when its triggering pattern appears in a workflow without foundation revision. | mech, `req:AR-033` | `ar-032` | |
| `ar-034` | Per-role definition scope (purpose + typical actions + exclusions) | Per AR-034: this spec MUST define per role: purpose, typical actions, what it does not do. Concrete permission schemas are owned by control-points. AR describes WHAT a role IS; control-points describes WHAT A ROLE IS ALLOWED TO DO. Specs do not duplicate. | mech, `req:AR-034` | `ar-032` | |
| `ar-036` | Merge responsibility is not a top-level role | Per AR-036: merge responsibility MUST NOT be a top-level role. A workflow MAY contain a distinct merge node assigned to whatever agent type the policy names. Merge node MUST operate in the run's leased worktree (per workspace-model), not a new worktree. Merge is a node function, not a role. | mech, `req:AR-036` | `ar-032` | |
| `ar-038` | Centralized-controller is the explicit inverse of Gas Town decentralization | Per AR-038: the centralized-controller invariant (AR-INV-007) is the explicit inverse of Gas Town polecats/mayors. Proposals introducing file-based agent-to-agent handoff or per-agent worktree ownership for the same run fail this principle and MUST be rejected or refactored. | mech, `req:AR-038` | (none) | Constrained by AR-INV-007 sensor. |
| `ar-039` | Merge agent operates in the run's worktree | Per AR-039: a merge agent (when workflow uses a distinct merge node) MUST operate in the same worktree the implementer used; the worktree is leased by the workflow run, not by any individual agent. | mech, `req:AR-039` | `ar-036` | |
| `ar-040` | Acknowledged centralized-controller tradeoff (no graceful degradation) | Per AR-040: the centralized-controller principle carries a real cost — if the daemon dies mid-run, every agent goes silent and reconciliation is required on restart. A decentralized alternative (Gas Town) would offer graceful degradation; harmonik foregoes that property in favor of routing simplicity. Acceptable within the MVH envelope (single-user, single-project, dev-machine). Pivot to decentralized requires re-evaluating under remote-daemon / multi-operator-single-daemon scenarios; pivot is a foundation amendment. | mech, `req:AR-040` | `ar-020` | OQ-AR-004 tracks additional pivot triggers; not a bead. |
| `ar-041` | Repository as single source of truth | Per AR-041: every normative artifact (specs, policies, workflow DOT files, skill registries, conventions) MUST live in the repository. External wikis, out-of-band knowledge bases, tribal-knowledge channels MUST NOT be load-bearing for any spec's conformance. | mech, `req:AR-041` | (none) | |
| `ar-042` | Invariants MUST name their sensor | Per AR-042: every invariant (`<prefix>-INV-NNN` block) MUST name its sensor inline within the invariant block or in §10.2 cross-reference. Sensor MAY be a lint, a verification-node role-function (per AR-029), a review-agent scenario, or a conformance test cited by ID. Spec-draft-time-only sensors count; runtime-only is not required. "TBD" / "reviewer judgment" without further detail fails AR-042. | mech, `req:AR-042` | `ar-029` | Meta-rule about all `<prefix>-INV-NNN` blocks; constrains AR's own invariants and every downstream spec's invariants. |
| `ar-043` | Constrain to empower (no exemption "to move faster") | Per AR-043: strict architectural boundaries (four-axis tags, mechanism/cognition split, subsystem envelope, three-artifact separation) are productivity multipliers and MUST be enforced. A spec asking for an exemption "to move faster" MUST be rejected; remedy is a foundation amendment per AR-020, not a local escape hatch. | mech, `req:AR-043` | `ar-001`, `ar-005`, `ar-013`, `ar-020` | |
| `ar-044` | Filesystem-backed coordination (no agent-conversation-as-state) | Per AR-044: agent coordination artifacts (session logs, transition-record sibling files, skill manifests, policy YAML) MUST be filesystem-backed; conversations between agents are ephemeral and MUST NOT be load-bearing for any state the daemon consumes. The daemon reads state from filesystem; not from agent conversation transcripts. | mech, `req:AR-044` | (none) | |
| `ar-045` | Quality-left ordering (fast deterministic before expensive inferential) | Per AR-045: fast deterministic checks (lint, type-check, policy-check) MUST run before expensive inferential checks (review agent, scenario run) wherever a workflow can admit both. Default ordering; workflow authors MAY override with explicit DOT annotation citing a policy reason. | mech, `req:AR-045` | (none) | |
| `ar-047` | Spec artifact definition | Per AR-047: a `spec` is a kerf output landing in `specs/` at the repo root. Design-and-thinking artifact: what, why, acceptance criteria. Specs are normative for system behavior. See template in `docs/foundation/spec-template.md`. | mech, `req:AR-047` | (none) | Artifact #1 of three. |
| `ar-048` | Workflow-graph artifact definition | Per AR-048: a `workflow graph` is a DOT document. Normative description of HOW execution happens: nodes, edges, policies by reference. Authored in workflow library, reviewed as code, versioned alongside spec corpus. | mech, `req:AR-048` | (none) | Artifact #2 of three. |
| `ar-049` | Bead artifact definition | Per AR-049: a `bead` is an atomic queued work item in the Beads SQLite store. The claimable unit the daemon dispatches a run against. | mech, `req:AR-049` | (none) | Artifact #3 of three. |
| `ar-050` | Many-to-many relationships without projection | Per AR-050: relationships among the three artifacts MUST be many-to-many without projection — a spec MAY describe zero/one/many workflows and beads; a workflow MAY be invoked by many beads; a bead references the workflow and node it executes and carries a bead ID appearing in checkpoint-commit trailers and event payloads. | mech, `req:AR-050` | `ar-047`, `ar-048`, `ar-049` | |
| `ar-051` | "Feature" is not a product primitive | Per AR-051: "feature" MUST NOT appear as a product primitive in any spec. Work size varies by node/sub-graph composition, not by a `feature` entity. Specs/workflows/beads are the normative vocabulary; "feature" at most casual-speech, never normative weight. | mech, `req:AR-051` | `ar-047`, `ar-048`, `ar-049` | Constrained by AR-INV-008 sensor. |

**Per-requirement bead count:** 50 active §4 reqs map to 49 distinct first-class beads (AR-035 collapsed to a `notes:` line on `ar-026` per discipline §2.1a; the bead carries co-tags `req:AR-026, req:AR-035`). **Zero §2.3 coalesces** in the strict sense (no letter-suffix sub-cases like `BI-010a/b`); the AR-026/AR-035 collapse uses §2.1a (pure cross-reference exception, new in discipline v0.5) which mirrors §2.3's tag-co-existence treatment. **Zero multi-step splits** (no §4 requirement contains a numbered step list — AR is meta-rule / declaration-heavy). **Zero step beads.** **Total req beads = 49.**

Retired §4 IDs (NOT minted as beads, enumerated in spec-parent description per discipline §2.11(b)): `AR-008` (v0.2.0; anti-pattern list moved to AR-007 EXAMPLE), `AR-037` (v0.3.0; promoted to AR-INV-007), `AR-046` (v0.3.0; promoted to AR-INV-008).

---

## 3. Sensor / invariant task table

One bead per active `AR-INV-NNN` per discipline §2.5. Sensor↔impl edges are **one-way**: sensor blocks-on impl, never the reverse (per discipline §2.5 F12).

| bead-id | title | description | tags | blocks edges (citing bead → prerequisite) | notes |
|---|---|---|---|---|---|
| `ar-inv-001` | Sensor: mechanism/cognition split is strict at the process boundary | Per AR-INV-001: daemon process MUST carry only mechanism-tagged logic; all cognition-tagged evaluation MUST occur in agent handler subprocesses. **Sensor (reviewer-enforced corpus-search heuristic):** conformance-auditor reviewer persona (per build-practices.md §Agent review) scans every cognition-tagged requirement in the corpus and verifies the declared delegation path targets an agent handler subprocess per §4.5.AR-017(a), not an in-daemon code path. The heuristic is reviewer-enforced because the spec template carries no machine-readable daemon-vs-subprocess classification per requirement (lint cannot resolve "in-daemon" vs "in-subprocess" without a `Process:` tag). Known false-negatives tracked in OQ-AR-005; post-MVH `Process:` tag candidate amendment. | mech, `req:AR-INV-001`, `kind:invariant` | `ar-003`, `ar-005`, `ar-006`, `ar-007`, `ar-016`, `ar-017`, `ar-042` | Constrained reqs span the ZFC tagging trio (AR-005/006/007) and the daemon-process-boundary pin (AR-016/017). v0.2.0: added `ar-003`, `ar-042` per discipline §2.5 reviewer-persona-bundling clause (new in v0.5) — AR §10.2's conformance-auditor persona explicitly bundles AR-INV-001 with AR-003, AR-007, AR-042; `ar-007` was already present, `ar-003` and `ar-042` now added. One-way only — impl beads do NOT block on this sensor (per discipline §2.5 F12). |
| `ar-inv-003` | Sensor: search + verifier + traces are required, not optional | Per AR-INV-003: any harmonik deployment claiming foundation conformance MUST exhibit search (AR-010), verification (AR-011), and traces (AR-012). A deployment eliding any of the three is non-conforming. **Sensor:** corpus presence test per §10.2 AR-009..AR-012 group — verifies that search representation (transition kinds in execution-model), verifier representation (verification-node role-function in execution-model), and trace representation (Transition record in execution-model) all appear in the corpus. | mech, `req:AR-INV-003`, `kind:invariant` | `ar-009`, `ar-010`, `ar-011`, `ar-012` | Sensor is a corpus presence test; the constrained reqs declare the three surfaces. |
| `ar-inv-007` | Sensor: centralized-controller invariant | Per AR-INV-007: harmonik MUST be a centralized-controller system. The deterministic daemon (Go binary, no LLM logic) MUST own all workflow state, routing, dispatch. Agents MUST perform only cognitive work. Agent-to-agent coordination MUST route through the daemon. File-based or ad-hoc-IPC agent-to-agent coordination is forbidden. **All cross-subsystem registries (policy, control-point, handler, skill) are daemon-owned and in-process for MVH**; relocating a registry outside the daemon requires the AR-020 amendment protocol. **Sensor (reviewer-agent scenario test, per §10.2 AR-038..AR-045 group):** proposals introducing file-based agent handoff, per-agent worktree ownership for the same run, or out-of-daemon cross-subsystem registry are rejected. | mech, `req:AR-INV-007`, `kind:invariant` | `ar-038`, `ar-039`, `ar-040`, `ar-041`, `ar-042`, `ar-043`, `ar-044`, `ar-045` | Promoted from retired AR-037 at v0.3.0. Constrained reqs are §4.9 harness-engineering invariants. |
| `ar-inv-008` | Sensor: three-artifact separation | Per AR-INV-008: harmonik uses exactly three artifacts (`spec`, `workflow graph`, `bead`) for work composition. No artifact MAY be treated as a projection of another. No spec MAY introduce a fourth compositional artifact (e.g., `feature`, `story`, `epic`, `capability-plan`, `initiative`). "Compositional" = a durable, authored artifact carrying work-shape; configuration artifacts (policy YAML, skill manifests, `.harmonik/*` sidecars) are not compositional and are exempt. **Sensor:** corpus-lint per §10.2 AR-047..AR-051 group (`feature` is non-normative; no fourth compositional term introduced). | mech, `req:AR-INV-008`, `kind:invariant` | `ar-047`, `ar-048`, `ar-049`, `ar-050`, `ar-051` | Promoted from retired AR-046 at v0.3.0. Sensor is corpus-lint over §4.10 reqs. |

**Sensor-bead count:** 4.

Retired §5 IDs (NOT minted as beads, enumerated in spec-parent description per §2.11(b)): `AR-INV-002`, `AR-INV-004`, `AR-INV-005`, `AR-INV-006` (all retired at v0.2.0 as duplicates of §4 reqs per template selection test).

---

## 4. Schema / error-taxonomy task table

Per discipline §2.6: every `RECORD`, `INTERFACE`, or `ENUM` in §6 becomes its own bead. AR's §6 is unusual — it explicitly defers all type schemas to downstream owning specs and declares only one data shape: the agent-type identifier regex in §6.1. There is no §8 error taxonomy.

| bead-id | title | description | tags | blocks edges (citing bead → prerequisite) | notes |
|---|---|---|---|---|---|
| `ar-schema.agent-type-identifier` | Define `agent_type` identifier regex (§6.1) | Implement the agent-type identifier regex `agent_type := ^[a-z][a-z0-9-]{1,62}$` plus the four reserved MVH identifiers `{claude-code, pi, claude-twin, pi-twin}`. AR-025 and AR-027 cite this shape; AR-027 requires byte-for-byte equality across four cross-subsystem surfaces (YAML policies, DOT node attributes, `LaunchSpec.agent_type`, event payloads). | mech, `kind:primitive-shape` | (none) | AR's only data-shape declaration. Strictly speaking neither RECORD nor INTERFACE nor ENUM — a constrained string primitive (regex). v0.2.0: retagged from `kind:schema` to `kind:primitive-shape` per discipline §2.6 (v0.5) which added the constrained-primitive schema category. F-pilot-AR-3 closed by this discipline patch. |

**Schema + error-taxonomy bead count:** 1 (1 schema + 0 error taxonomy).

(Per discipline §2.11(c), §8 error taxonomies with 11+ categories like RC's would mint one bead per category. AR has no §8 at all, so this case does not apply.)

---

## 5. Cross-spec edge summary

These are the cross-spec `blocks` edges this pilot would emit, walked from inline cites in normative prose (§4 / §5 / §6) per discipline §2.7 + §3.1.

**Result: ZERO outbound cross-spec edges from AR.**

Rationale and evidence:

1. **AR's front-matter `depends-on: []`** (verified at line 16 of `specs/architecture.md`). Per discipline §3.2, every cross-spec edge target MUST appear in the source spec's `depends-on`. With an empty `depends-on`, the universe of admissible edge targets is empty.
2. **AR is the root of the foundation corpus.** §9.1 explicitly states "None. This spec is the root of the foundation corpus." Every other foundation and subsystem spec depends on AR; AR depends on nothing.
3. **All cross-document cites in AR's body resolve to `docs/foundation/components.md §N`** — a bootstrap-stub document, not a finalized spec. Per the §9.3 informative note, these cites are bootstrap-only and migrate to the actual spec reference within one revision cycle after the target spec is finalized. Per discipline §3.1, edges are derived from `[<other-spec-id>.md §N]` cites; `docs/foundation/components.md` is not a spec ID. These cites do not generate edges.
4. **§9.3 co-reference entries generate no edges** per discipline §2.7 third class.
5. **§A.1 envelope exemplar references** (`[event-model.md §6.3]`, `[handler-contract.md §N]`, `[execution-model.md §6.1]`, `[control-points.md §N]`, `[operator-nfr.md §N]`) are inside the appendix and per discipline §3.1 / §5 generate no edges.
6. **§12 revision history and §11 OQs** generate no edges per discipline §3.1.
7. **INFORMATIVE / EXAMPLE / RATIONALE blocks** scattered through AR (under AR-004, AR-007, AR-027, §5 lead, §A.3) generate no edges.

**`depends-on` validation:** `depends-on: []` is consistent with zero outbound edges. The empty list IS the depends-on universe.

**Cycle check (intra-AR only):** The intra-spec edges in §2 / §3 / §4 form a DAG. Spot-check of dense regions:
- AR-INV-001 sensor → {AR-003, AR-005, AR-006, AR-007, AR-016, AR-017, AR-042} — none of these block back on AR-INV-001 (one-way per §2.5 F12). No cycle.
- AR-INV-003 sensor → {AR-009, AR-010, AR-011, AR-012} — same one-way pattern. No cycle.
- AR-INV-007 sensor → {AR-038…AR-045} — same one-way pattern. No cycle.
- AR-INV-008 sensor → {AR-047…AR-051} — same. No cycle.
- AR-013 → {AR-001, AR-005, AR-052}; AR-052 → {AR-016}; AR-016 has no inbound from AR-013 cycle peers. No cycle.
- AR-053 → {AR-052, AR-013}; reverse cites (AR-013 → AR-053 and AR-052 → AR-053) reclassified as informational per discipline §2.7 F13 slot-rule heuristic — no edges emitted. No cycle.
- AR-021 (cognition) → {AR-020, AR-032} — AR-032 does not cycle back. No cycle.
- AR-029 → {AR-032}; AR-011 → {AR-030, AR-032}; AR-030 → {AR-029, AR-005}; AR-031 → {AR-030, AR-029}. v0.2.1 dropped the invented edge `ar-011 → ar-029` (F-pilot-AR-9). v0.2.2 dropped the "per X" supporting-cite edges `ar-029 → ar-011` and `ar-029 → ar-018` (F-pilot-AR-10) — these were treated as hard deps in v0.2.1 but cite a stand-alone assertion ("No 'verifier' subsystem exists") that is independently testable. The 3-cycle `ar-011 → ar-030 → ar-029 → ar-011` (all three steps from inline cites) was surfaced at load attempt; v0.2.2 broke it by reclassifying `ar-029 → ar-011` as informational. No cycle in v0.2.2. (See §8 F-pilot-AR-10 for the supporting-cite-vs-hard-dep heuristic to be codified in discipline v0.6.)
- AR-007 → {AR-005, AR-032}; AR-032 does not block on AR-007. No cycle.

(A full cycle check is properly the load-time `br dep cycles` invocation; the pilot's structural inspection above is the pre-load sanity pass.)

---

## 6. Optional infrastructure tasks

Per discipline §2.4, shared test infrastructure across multiple impl beads is extracted as a separate bead. AR's §10.2 names corpus-level lints (four-axis grammar, ZFC tagging, agent_type regex, role-name set, three-artifact `feature` non-normativity), corpus presence tests (search/verifier/traces), and reviewer-persona scans (architect, conformance-auditor, critic, scope-steward).

**No infrastructure beads minted at the AR-pilot level.** Rationale:

- **Reviewer-persona scans** (architect, conformance-auditor, critic, scope-steward) are not test infrastructure shared across AR impl beads; they are reviewer roles consumed at corpus-wide spec-review time. Their authoritative locus is `docs/components/internal/build-practices.md §Agent review`, not AR. No AR bead "uses" the scan in the sense §2.4 requires.
- **Corpus-level lints** (e.g., "every requirement carries `Tags:`", "every cross-subsystem type carries Axes") are themselves the work named by AR-001/AR-005/etc.; they are the impl beads, not shared fixtures the impl beads depend on.
- **Corpus presence tests** for AR-INV-003 (search/verifier/traces) reduce to the §3 sensor bead `ar-inv-003`; no separate harness gates AR-009..AR-012 work.
- **AR has no crash-injection or cross-subsystem scenario harness analogue** to BI's `bi-test.crash-harness`.

If, post-MVH, a corpus-lint runner emerges as a shared CI artifact that AR-001/AR-005/AR-013/AR-027/AR-032/AR-051 all consume, that may be extracted then. At MVH it does not exist as a separate work item.

**Test-infra bead count:** 0.

---

## 7. Tally

### AR bead count

| Class | Count |
|---|---|
| Spec parent bead (`ar`) | 1 |
| Requirement beads (1 §2.1a collapse: AR-035 → notes on `ar-026`; no step splits) | 49 |
| Step beads | 0 |
| Sensor / invariant beads | 4 |
| Schema beads (1 `kind:primitive-shape`) | 1 |
| Error-taxonomy beads | 0 |
| Test-infrastructure beads | 0 |
| **Total AR beads** | **55** |

**Arithmetic check:** 1 + 49 + 0 + 4 + 1 + 0 + 0 = **55**. ✓ (v0.1.0 reported 56; v0.2.0 drops 1 from AR-035 collapsing to a notes line on `ar-026` per discipline §2.1a.)

### How AR compares to BI

| Class | BI (v0.1.3) | AR (v0.2.0) | Delta-rationale |
|---|---|---|---|
| Spec parent | 1 | 1 | — |
| Req beads | 40 | 49 | BI: 43 reqs − 3 coalesces = 40. AR: 50 reqs − 1 §2.1a collapse (AR-035 → notes on `ar-026`) = 49. |
| Step beads | 11 | 0 | AR has zero multi-step protocol requirements (declaration-heavy spec). |
| Sensors | 4 | 4 | Coincidentally matched. |
| Schemas | 8 | 1 | AR §6 explicitly defers nearly all type schemas to downstream specs; declares only the agent-type regex (now `kind:primitive-shape`). |
| Error taxonomy | 1 | 0 | AR has no §8. |
| Test-infra | 1 | 0 | AR has no shared fixture analogue. |
| **Total** | **66** | **55** | AR multiplier ≈ 1.10× requirement→total (50 → 55). |

The AR multiplier (~1.10×) is materially lower than BI's (~1.53×). Two structural reasons:

1. **AR has no multi-step protocols.** AR is a meta-rule and declaration spec; it specifies what other specs MUST contain, not protocols the daemon executes. Zero step-bead inflation.
2. **AR defers nearly all type schemas downstream.** §6.1 owns one regex; the rest of §6 is a defer table. BI owns 8 §6 schemas; AR owns 1.

If the BI extrapolation projection in `bi-pilot.md` §7 ("AR ≈ 70 beads, ~1.3×") was reading AR's 52 numerically-listed reqs without subtracting retired-but-present rows or accounting for AR's missing §8 / minimal §6, the actual 55-bead count (v0.2.0 after §2.1a collapse) is consistent with the lower-multiplier intuition the BI pilot named. This shifts the corpus extrapolation modestly downward; the discipline author may want to revisit the per-spec rough estimates after AR + EM (the next pilot) land — see §8 item below.

---

## 8. Items in AR that did NOT decompose cleanly

These are notable rough edges from the AR pilot for the discipline author. Each is tagged `F-pilot-AR-N` for the reviewer triage step. v0.2.0 marks several findings closed by the discipline v0.5 patch (which was driven by these and the r1 review's class findings); see `docs/reviews/2026-04-27-ar-pilot-r1/synthesis.md` for the lane-by-lane resolution.

1. **F-pilot-AR-1 — Pure cross-reference requirements (AR-035). [CLOSED v0.5]** AR-035 says "Role is orthogonal to agent type — see §4.7.AR-026 for the normative rule." It is a navigation aid, not a normative rule. **Resolution:** discipline v0.5 added §2.1a (pure cross-reference exception); v0.2.0 collapses AR-035 to a `notes:` line on `ar-026` with co-tag `req:AR-035`. Drops first-class bead count 50 → 49, total 56 → 55.

2. **F-pilot-AR-2 — `docs/foundation/components.md §N` bootstrap stubs are not spec cites. [RETAINED — carry-forward TODO, not a discipline gap.]** AR's normative prose (e.g., AR-010, AR-011, AR-016, AR-017, AR-024, AR-027) cites `[docs/foundation/components.md §2]`, `[docs/foundation/components.md §4]`, `[docs/foundation/components.md §6]`, `[docs/foundation/components.md §8]`, `[docs/foundation/components.md §9]`, `[docs/foundation/components.md §10]`. These are template-§Cross-reference-convention bootstrap stubs that migrate to actual spec cites within one revision cycle after each target spec finalizes. Per discipline §3.1, edges are emitted from `[<other-spec-id>.md §N]` cites; `docs/foundation/components.md` is not a spec ID. The pilot emits no edges from these cites (correct per discipline; codified in discipline v0.5 §3.1 no-edge-list expansion under F-pilot-AR-AO3). **However:** if spec authors fail to migrate these stubs once EM/EV/HC/CP/PL/RC/BI finalize, AR's outbound edge graph will remain artificially zero forever, and the `depends-on: []` validation will silently mask missing edges. r1 synthesis decision: this is not a discipline gap (`docs/foundation/components.md` cites are correctly non-edge-generating); the migration-debt tracking is a meta-concern about when AR's cites should be rewritten as the target specs finalize. **Carry-forward TODO:** when EM/EV/HC/CP/PL/RC/BI/ON specs land in `reviewed` status, AR's bootstrap-stub cites must be migrated to real spec cites within one revision cycle, and AR's `depends-on: []` updated accordingly. Tracked here pending a separate carry-forward mechanism in the decompose-to-tasks track.

3. **F-pilot-AR-3 — `RECORD`/`INTERFACE`/`ENUM` typology gap for primitive shapes. [CLOSED v0.5]** AR §6.1 declares one data shape: `agent_type := ^[a-z][a-z0-9-]{1,62}$`. This is neither a `RECORD` nor an `INTERFACE` nor an `ENUM` (regex constraint over open identifier space). **Resolution:** discipline v0.5 §2.6 added "constrained primitive types" as a fourth schema category with tag `kind:primitive-shape`. v0.2.0 retags `ar-schema.agent-type-identifier` from `kind:schema` to `kind:primitive-shape`.

4. **F-pilot-AR-4 — Cognition-tagged delegation path quote in description (AR-021). [ACCEPTED at current length.]** AR-021 is the only cognition-tagged AR requirement. Per discipline §2.8, the bead description MUST quote the delegation path (role + model class + input shape). The pilot does this in the `ar-021` row by inlining the full clause from AR-021's body. The resulting description is long (≈300 chars). Discipline §4 tag mapping table caps title at ≤80 chars but does not cap description length. r1 review did not escalate; v0.2.0 retains the description verbatim as expected behavior for cognition-tagged beads.

5. **F-pilot-AR-5 — `foundation-cross-cutting` self-classification creates AR-013/052/053 self-reference. [ACCEPTED — expected behavior for foundation-cross-cutting specs.]** AR is itself `foundation-cross-cutting` per its own front matter; AR-013 (envelope content) and AR-053 (envelope slot) do not apply to AR. The bead-level edge graph is internally consistent (`ar-013 → ar-052`, `ar-053 → {ar-052, ar-013}`) — these gate the AR-defined rules, not AR-as-instance conformance. r1 review confirmed this is expected behavior; the same shape will recur in any other foundation-cross-cutting spec (operator-nfr, beads-integration, reconciliation, testing) and the discipline already handles it (no §4.a slot for foundation-cross-cutting specs).

6. **F-pilot-AR-6 — Open questions OQ-AR-001..OQ-AR-007 are not bead-loaded but include real impl-adjacent work. [INFORMATIONAL — discipline §2.8 F6 carve-out covers OQ resolution.]** Specifically: OQ-AR-005 names a candidate `Process:` template tag that would mechanize AR-INV-001's reviewer-enforced sensor; OQ-AR-007 names a candidate mechanical test for spec-category assignment. Both are spec-template-amendment + lint-implementation work that, if the OQs resolve, mint new beads per discipline §2.8 F6 carve-out (`kind:spec-edit` + `kind:impl`). Pilot does not pre-mint placeholder beads (correct); flags here so reviewer triage knows AR has 7 OQs vs BI's 14.

7. **F-pilot-AR-7 — AR retirement count and §1 spec-parent description scope. [INFORMATIONAL — no cap violated.]** AR has 7 retired IDs (3 §4 + 4 §5) vs BI's 0. Spec-parent description enumerates all 7 per discipline §2.11(b). Discipline does not cap spec-parent description length; v0.2.0 retains the enumeration verbatim.

8. **F-pilot-AR-8 — Bidirectional inline cites in the envelope-slot trio. [CLOSED v0.5]** AR-053 (slot rule) inline-cites AR-013 (envelope content) and AR-052 (subsystem categorization). AR-013 inline-cites AR-052 ("§4.0.AR-052") and AR-053 ("§4.a slot reserved by §4.0.AR-053"). AR-052 inline-cites AR-053 ("envelope per AR-053") and AR-016 ("Go-package inside the daemon binary per §4.5.AR-016"). The naive edge derivation produces two 2-cycles (AR-013 ↔ AR-053 and AR-052 ↔ AR-053). **Resolution:** discipline v0.5 §2.7 added the F13 slot-rule/content-rule heuristic — when A defines a slot and B defines the content, A→B is the normative dep and B→A is informational plumbing. AR-053 is the slot rule; AR-013 (8 envelope elements) and AR-052 (subsystem categorization that names the slot obligation) fill it. v0.2.0 emits `ar-053 → {ar-013, ar-052}` and reclassifies the reverse cites `ar-013 → ar-053` and `ar-052 → ar-053` as informational (no edge). The non-bidirectional half AR-052 → AR-016 is emitted normally. F-pilot-AR-2.5/2.6/2.7 (term-use edges) and F-pilot-AR-AO1/AO2/AO3 (no-edge list expansion) and F-pilot-AR-2.8 (§10.2 reviewer-persona bundling) are also closed by discipline v0.5; see `docs/reviews/2026-04-27-ar-pilot-r1/synthesis.md` §2.2.

10. **F-pilot-AR-10 — Supporting cite vs hard dep (NEW, surfaced at load attempt; CLOSED v0.2.2).** During the v0.2.1 → Beads load attempt, `br dep cycles` rejected `ar-030 → ar-029` because it closed a 3-cycle `ar-011 → ar-030 → ar-029 → ar-011`. All three edges traced to inline cites: AR-011 cites `§4.7.AR-030` ("see §4.7.AR-030"); AR-030 cites AR-029 ("(per AR-029)" attached to the verification-result definition); AR-029 cites AR-011 ("per §4.5.AR-018 and §4.3.AR-011" attached to "No 'verifier' subsystem exists"). The first two are hard deps (the citing rule cannot be tested without the cited rule's content/concept). The third is a **supporting cite** — AR-029's main claim ("no `verification-node` enum value") is independently testable; "per AR-018 and AR-011" cites two prior rules as consistency back-references for a stand-alone secondary assertion ("No 'verifier' subsystem exists"). **Heuristic for discipline v0.6:** an inline cite "per X" attached to a stand-alone assertion (an assertion that remains testable if the cite is removed) is a *supporting cite* and reclassifies as informational (no edge). Distinguished from "uses X's shape/value/concept" cites which are hard deps. **Fix applied in v0.2.2:** dropped `ar-029 → ar-011` and `ar-029 → ar-018` (both "per X" supporting cites attached to "No 'verifier' subsystem exists"). Cycle broken. AR-029's surviving edge is `ar-032` (term-use of "Reviewer" role). Class-level finding for v0.6: discipline §3.1 step 1 should grow a sub-clause distinguishing supporting cites from hard deps.

9. **F-pilot-AR-9 — AR-011 ↔ AR-029 2-cycle (CLOSED v0.2.1).** Source-spec audit during v0.2.0 re-draft showed: AR-029 inline-cites AR-011 ("No 'verifier' subsystem exists per §4.5.AR-018 and §4.3.AR-011" — explicit). AR-011 inline-cites AR-030 ("see §4.7.AR-030") but does NOT inline-cite AR-029 (verified by `grep "AR-029"` over architecture.md — AR-029 appears at lines 275 (own header), 283 (in AR-030 body), 359 (in AR-042 body), 580 (revision history), 644 (rationale §A.3); not in AR-011's body lines 156–160). Pilot v0.1.0 carried `ar-011 → {ar-029, ar-030, ar-032}`; the `ar-029` edge was an invented citation-projection. r1 review's BLOCKERs (2.1: drop `ar-029 → ar-016`; 2.2: drop `ar-035 → ar-032`) caught two analogous invented edges but missed this third one. **Fix applied in v0.2.1:** dropped `ar-011 → ar-029` edge (one-way: AR-029 → AR-011 only); the pair is conceptually a peer-restatement (both rules say verification-is-a-role-function, AR-011 rules out "verifier as subsystem" while AR-029 rules out "verifier as node-type-enum-value"), not a slot-rule/content pair, so F13 did not apply. The 2-cycle would have failed the load-time `br dep cycles` check; v0.2.1 is now load-clean.

---

## 9. Revision history

| Date | Version | Author | Summary |
|---|---|---|---|
| 2026-04-27 | 0.2.2 | foundation-author | Pilot-lane patch on top of v0.2.1 to close F-pilot-AR-10 (3-cycle `ar-011 → ar-030 → ar-029 → ar-011` surfaced when `br dep cycles` rejected `ar-030 → ar-029` during the v0.2.1 load attempt). All three edges traced to inline cites; resolution per a new "supporting cite vs hard dep" heuristic (to be codified in discipline v0.6 alongside r2's three findings): an inline cite "per X" attached to a stand-alone assertion that is independently testable is informational, not a hard dep. AR-029's body cites "per §4.5.AR-018 and §4.3.AR-011" attached to "No 'verifier' subsystem exists" — AR-029's main enum claim is independently testable, so both `ar-029 → ar-011` and `ar-029 → ar-018` reclassified as informational and dropped. AR-029's surviving block edge is `ar-032` (term-use of "Reviewer" role). §5 cycle-check rewritten; §8 F-pilot-AR-10 added; ar-029 row blocks col updated. No structural changes to bead count (still 55), tally (still 55), or any other rows. |
| 2026-04-27 | 0.2.1 | foundation-author | Pilot-lane patch on top of v0.2.0 to close F-pilot-AR-9 (AR-011 ↔ AR-029 2-cycle surfaced during v0.2.0 re-draft). Dropped invented edge `ar-011 → ar-029` (AR-011's body inline-cites only `§4.7.AR-030`; the AR-029 reference in v0.1.0 was a citation-projection error analogous to r1's BLOCKERs 2.1 and 2.2 but uncaught at r1). §5 cycle-check rewritten to reflect resolution; §8 F-pilot-AR-9 marked CLOSED v0.2.1; ar-011 row notes updated. No structural changes to bead count (still 55), tally (still 55), or any other edges. r2 reviewer pass should run against v0.2.1, not v0.2.0. |
| 2026-04-27 | 0.2.0 | foundation-author | Re-drafted against discipline v0.5 after r1 review (synthesis at `docs/reviews/2026-04-27-ar-pilot-r1/synthesis.md`). Pilot-lane fixes: M1 §1 narrative miscount ("5 retired IDs" → "7 retired IDs total"; removed mid-sentence self-correction); 2.1 dropped invented edge `ar-029 → ar-016` (AR-029 inline-cites AR-011 + AR-018 only); 2.2 collapsed AR-035 to a notes-line on `ar-026` per discipline §2.1a (drops first-class bead count 50 → 49, total 56 → 55; the original 2.2 fix dropping `ar-032` from `ar-035` blocks is moot once AR-035 is collapsed). Discipline-v0.5 application: F13 slot-rule heuristic resolved AR-013/AR-052/AR-053 envelope-slot trio (`ar-053 → {ar-013, ar-052}` survive; reverse cites `ar-013 → ar-053` and `ar-052 → ar-053` reclassified as informational; `ar-052 → ar-016` added as the non-bidirectional half of synthesis 2.4); term-use edges walked across 49 active reqs and emitted (delta: 9 new term-use edges — `ar-052 → ar-016`, `ar-004 → ar-013`, `ar-014 → ar-005`, `ar-017 → ar-013`, `ar-019 → ar-013`, `ar-028 → ar-016`, `ar-029 → ar-032`, `ar-030 → ar-005`, `ar-031 → ar-029`); `ar-schema.agent-type-identifier` retagged from `kind:schema` to `kind:primitive-shape` per discipline §2.6 (v0.5); `ar-inv-001` sensor edges expanded with §10.2 conformance-auditor bundle (added `ar-003`, `ar-042`; `ar-007` was already present). §8 marks F-pilot-AR-1 / -3 / -8 closed via discipline v0.5; F-pilot-AR-2.5/2.6/2.7 (term-use), F-pilot-AR-AO1/AO2/AO3 (no-edge list), F-pilot-AR-2.8 (§10.2 bundling) all closed via discipline v0.5; F-pilot-AR-2 (bootstrap-stub cite migration debt) retained as a carry-forward TODO (not a discipline gap). New finding F-pilot-AR-9 surfaced during re-draft: AR-011 ↔ AR-029 2-cycle (pilot v0.1.0 carried invented edge `ar-011 → ar-029` analogous to r1's 2.1/2.2 BLOCKERs but uncaught by r1; v0.2.0 retains the v0.1.0 edge to avoid over-stepping the synthesis instruction set, surfaces explicitly in §5 cycle-check and §8 for r2 reviewer fix — the cycle WILL fail `br dep cycles` and is a hard prerequisite to loading). |
| 2026-04-27 | 0.1.0 | foundation-author | Initial AR pilot draft against `specs/architecture.md` v0.3.1. Decomposed against discipline v0.4. **50 active reqs / 4 invariants / 1 schema / 0 error taxonomy / 0 multi-step splits / 0 coalesces. Total: 56 beads** (1 spec-parent + 50 req + 0 step + 4 sensor + 1 schema + 0 taxonomy + 0 infra). Zero outbound cross-spec edges (AR `depends-on: []` — root of foundation corpus). Seven §8 rough edges flagged (`F-pilot-AR-1` through `F-pilot-AR-7`) for reviewer triage: pure cross-reference req (AR-035), bootstrap-stub cite class (`docs/foundation/components.md §N`), primitive-shape schema typology (regex), long cognition-bead descriptions (AR-021), foundation-cross-cutting self-classification chain (AR-013/052/053), 7 OQs proportional to spec size, retired-ID count in spec-parent description. |
