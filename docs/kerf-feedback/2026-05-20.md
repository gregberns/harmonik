# Kerf Feedback — 2026-05-20

Context: filed while drafting `docs/orchestration-protocol-v2.md`. Observations from running `kerf next` + `kerf triage` against the current corpus (207 open, 200 ready, 1456 total; baseline 2026-05-18T23:27:28Z).

## OBSERVATION: 156 untriaged beads is the dominant signal in `kerf next`

After 49 ranked dispatchable items, `kerf next` ends with `! 156 untriaged beads · ! 5 beads multi-matched · ! 127 beads changed externally`. That's 75% of the open corpus invisible to the prioritized feed because no work claims them. Two patterns:

1. **Many untriaged beads are kerf-upstream feedback** (hk-43ate, hk-5py8k, hk-51ivc, hk-2pwgv, hk-kx498, hk-ynd1l, ...). There is no kerf work to attach them to — they're feedback *about* kerf. The triage suggester correctly proposes `kerf pin bridge-integration <id>` for some but that's not the right home; they're a separate stream.
   **Suggested fix on the harmonik side:** create a work `kerf-upstream` with `bead-filter 'label=kerf-upstream'` to surface them as a coherent stream in `kerf next`. Will do this next session unless feedback says otherwise.

2. **`kerf triage` proposes `kerf new <codename>` for cross-cutting labels** (axis/tag/kind/scope) on hk-63oh.* beads — e.g. it proposes `kerf new reconciliation` and `kerf new operator-nfr` and `kerf new control-points` for ONE bead because that bead carries 6 spec labels. This is hk-u5ymq (already filed). Not a new issue; just re-confirming the pattern is common in the v51 corpus.

## OBSERVATION: 5 multi-matched warnings have an obvious mechanical fix

```
hk-cw56j  — kerf pin claude-hook-bridge hk-cw56j
hk-gerqr  — kerf pin claude-hook-bridge hk-gerqr
hk-qo96c  — kerf pin claude-hook-bridge hk-qo96c
hk-vh1bw  — kerf pin extqueue hk-vh1bw
hk-xlach  — kerf pin claude-hook-bridge hk-xlach
```

`kerf` could offer a `kerf triage --resolve-multi-matched` flag that auto-applies the suggester's recommended pin. Minor UX win, would clear all 5 warnings in one command. (Filing here, not as a new bead, since hk-n9gxx already covers triage UX.)

## OBSERVATION: kerf-upstream bug surface is large + recurring

The corpus carries at least 18 `label=kerf-upstream` beads (hk-43ate, hk-5py8k, hk-51ivc, hk-2pwgv, hk-kx498, hk-ynd1l, hk-or6gk, hk-8t3tc, hk-kb86i, hk-n9gxx, hk-rvbxi, hk-u5ymq, hk-el3yw, hk-ocarg, hk-1v09o, and 3+ more in untriaged). Each one is filed correctly (label set, body has reproducer). What's missing is a single rollup that lets the kerf maintainer pull them in one query, e.g. `kerf doctor --feedback` or just stable export. Suggestion to upstream: a `kerf feedback list --since <baseline>` command that emits the rollup. Until then, `br list --label kerf-upstream --status open` works.

## OBSERVATION: `kerf next` does NOT show priority

The ranked feed has 49 dispatchable beads but no priority column. The orchestrator has to cross-reference `br list --priority N` to know which top-ranked items are P1 vs P2 vs P3. Suggestion: add a `[P1]` / `[P2]` annotation in the text output, or a `--priority-filter` flag. This matters specifically for the "harmonik daily loop" — the agent should not have to run a second query to know what to dispatch.

## NON-ISSUE: `phase-3-dot` + `workflow-modes` empty works

`kerf next` flags these correctly as `empty   <work>   resolved bead_filter matches zero beads`. Intent-correct; just no eligible beads have been spawned yet. Will revisit when those subsystems generate work.

## Cross-reference

- The `kerf-upstream` work proposal above will be created and pinned in the next session if no objection.
- The "no priority in `kerf next`" observation is the single biggest friction for the v2 daily loop and is the most worth raising upstream.
- Full design context: `docs/orchestration-protocol-v2.md` (filed same date).

---

# Kerf Feedback — 2026-05-20 (session: testing-strategy-uplift planning)

Context: filed while creating `kerf new testing-strategy-uplift --jig plan` and advancing through problem-space → analyze → decompose → research (shelved).

## OBSERVATION: `kerf new --jig plan` jig works cleanly

`kerf new testing-strategy-uplift --jig plan` worked without friction. The jig printed the full pass overview and correct working directory path. No duplicated agent-instruction blocks observed (may have been fixed since the 2026-05-19 `kerf init` friction report). The `--jig plan` flag was accepted correctly.

## OBSERVATION: `kerf status <codename> <status>` does not validate artifact existence

Advancing `kerf status testing-strategy-uplift analyze` succeeded before `01-problem-space.md` was written (I wrote it after calling `kerf new`, then advanced status after writing). This means the jig advance does not check for artifact presence — the "what done looks like" checklist is advisory, not enforced. Minor gap; jig is self-documenting enough that agents can be disciplined without enforcement.

## OBSERVATION: plan jig `problem-space` pass asks for "2-3 conversational exchanges" — inappropriate for autonomous agent

The problem-space pass instructions say "Clarify what is changing and why through 2-3 conversational exchanges with the user." This is a human-in-the-loop model. In autonomous agent invocations (as here), the right behavior is to synthesize from context and write the artifact without waiting for confirmation. The jig's "Autonomous mode: Complete the full breakdown, then present for review" note in the decompose pass covers this for decompose, but the problem-space pass lacks an equivalent note. Suggestion: add "If autonomous, synthesize from provided context + codebase research; do not wait for user confirmation" to the problem-space pass instructions.

## NON-ISSUE: `kerf shelve` correctly prompts for SESSION.md

`kerf shelve testing-strategy-uplift` correctly reminded me to write SESSION.md. This is good behavior — the shelve command is a useful forcing function for agent session hygiene.
