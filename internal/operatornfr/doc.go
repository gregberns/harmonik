// Package operatornfr contains test fixtures and static-check tests for the
// Operator NFR spec (specs/operator-nfr.md). No production code lives here;
// all exported symbols are package-level test helpers that begin with the
// exitCodeFixture or obligationsFixture prefix per the implementer protocol.
//
// Governed by specs/operator-nfr.md; if any fixture value or constraint
// appears to conflict with the spec, the spec is authoritative.
package operatornfr
