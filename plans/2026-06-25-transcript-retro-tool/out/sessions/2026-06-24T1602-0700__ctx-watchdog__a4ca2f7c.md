# ctx-watchdog — a4ca2f7c

```
agent:            ctx-watchdog            (confidence 1.0)
session_id:       a4ca2f7c-ce76-489d-898f-52ce65616f51
file:             /Users/gb/.claude/projects/-Users-gb-github-harmonik/a4ca2f7c-ce76-489d-898f-52ce65616f51.jsonl
timeframe_local:  2026-06-24 16:02 → 2026-06-25 09:45  (UTC-7)
timeframe_utc:    2026-06-24T23:02Z → 2026-06-25T16:45Z
turns:            user=81  assistant=274  (tool_use=147)
model:            claude-sonnet-4-6
keeper_chain:     cleared_from=cold-boot  cleared_to=live
counts:           wakes=40  monitor=0  comms_out=38  schedulewakeups=15
marker_breakdown: --from:ctx-watchdog=309
```

## Cross-references

- Event bus: `.harmonik/events/events.jsonl` (agent_message / epic_completed / governor_signal)
- Live bus ledger: `harmonik comms log --json`
- comms `send` lines below are annotated `✓bus` (corroborated) / `⚠no-bus` (claimed but not on bus)

## Chronological digest (local | UTC)

[2026-06-24 16:02 | 23:02Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 16:02 | 23:02Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 16:02 | 23:02Z]  SAY  Setting up a 30-minute cron for the context-watchdog, then running the first tick now.
[2026-06-24 16:02 | 23:02Z]  SAY  Cron job `f9f23853` scheduled — every 30 min, auto-expires in 7 days. Running first tick now. _Runs until you close this session · For durable cloud-based loops, use /schedule_
[2026-06-24 16:03 | 23:03Z]  SAY  Admiral: 148k tokens (fresh) — OK. Captain: 125k tokens (fresh) — OK. Crews gurney/jamis/leto/paul/watch have no tmux session — skipped (already dead).
[2026-06-24 16:03 | 23:03Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ⚠no-bus
[2026-06-24 16:03 | 23:03Z]  SAY  Tick done. Self at 43k tokens (22% of 200k window) — well clear of 250k limit. **Summary:** - admiral: 148k / 1M — OK - captain: 125k / 1M — OK - gurney, jamis, leto, paul, watch: no tmux session — skipped (dead) - No restarts needed - Next tick fires in ~30 min (cron `f9f23853`)
[2026-06-24 16:44 | 23:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 16:44 | 23:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 16:44 | 23:44Z]  SAY  Flywheel has no .ctx — checking pane.
[2026-06-24 16:45 | 23:45Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ⚠no-bus
[2026-06-24 16:45 | 23:45Z]  SAY  Tick done. Self at 52k / 200k window — clear. - **admiral**: 99k/1M — OK - **watch**: 69k/200k — OK - **captain**: 54k/1M — OK - **flywheel**: live session, no `.ctx`, blank pane — idle, no keeper; not OVERSIZED/DEAD by the check rules, noted in summary - **paul**: no tmux session — skipped (dead)
[2026-06-24 17:14 | 00:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 17:14 | 00:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 17:14 | 00:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ⚠no-bus
[2026-06-24 17:14 | 00:14Z]  SAY  Tick done. Self at 56k/200k — clear. - **admiral**: 124k/1M — OK - **watch**: 95k/200k — OK - **captain**: 102k/1M — OK - **flywheel**: live, blank pane, no `.ctx` again — persistently idle/no keeper (noted two ticks in a row) - **paul**: dropped from crew list — gone
[2026-06-24 17:44 | 00:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 17:44 | 00:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 17:44 | 00:44Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ⚠no-bus
[2026-06-24 17:44 | 00:44Z]  SAY  Tick done. Self at 61k/200k — clear. - **admiral**: 132k/1M — OK - **watch**: 95k/200k — OK (same reading as last tick — keeper may be between updates) - **captain**: 103k/1M — OK - **flywheel**: live, blank pane, no `.ctx` — 3rd consecutive tick idle with no keeper
[2026-06-24 18:14 | 01:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 18:14 | 01:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 18:14 | 01:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ⚠no-bus
[2026-06-24 18:15 | 01:15Z]  SAY  Tick done. Self at 66k/200k — clear. - **admiral**: 77k/1M — OK (new session ID — was restarted between ticks) - **watch**: 95k/200k — OK (same reading 4th tick; keeper may batch updates) - **captain**: 119k/1M — OK - **flywheel**: live, no `.ctx`, 4th consecutive tick — persistently idle, no keeper attached
[2026-06-24 18:44 | 01:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 18:44 | 01:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 18:44 | 01:44Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ⚠no-bus
[2026-06-24 18:44 | 01:44Z]  SAY  Tick done. Self at 70k/200k — clear. - **admiral**: 121k/1M — OK - **watch**: 95k/200k — OK (same token count 5 ticks; keeper gauge appears sticky) - **captain**: 48k/1M — OK (new session ID, just cycled by its keeper) - **flywheel**: live, no `.ctx`, 5th consecutive tick — persistently idle
[2026-06-24 19:14 | 02:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 19:14 | 02:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 19:14 | 02:14Z]  SAY  New crew `gurney` joined. Checking all ctx files.
[2026-06-24 19:14 | 02:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ⚠no-bus
[2026-06-24 19:14 | 02:14Z]  SAY  Tick done. Self at 76k/200k — clear. - **admiral**: 81k/1M — OK - **gurney**: 112k/1M — OK (new crew, just started) - **watch**: 114k/200k — OK (57%, climbing — worth watching) - **captain**: 108k/1M — OK - **flywheel**: live, no `.ctx`, 6th consecutive tick idle
[2026-06-24 19:44 | 02:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 19:44 | 02:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 19:44 | 02:44Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ⚠no-bus
[2026-06-24 19:45 | 02:45Z]  SAY  Tick done. Self at 80k/200k — clear. - **admiral**: 81k/1M — OK - **gurney**: 117k/1M — OK - **watch**: 114k/200k — OK (57%, unchanged two ticks — keeper may be between updates) - **captain**: 108k/1M — OK (same reading, age 64s — borderline stale but under 5min) - **flywheel**: live, no `.ctx`, 7th consecutive tick idle
[2026-06-24 20:14 | 03:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 20:14 | 03:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 20:14 | 03:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-24 20:14 | 03:14Z]  SAY  Tick done. Self at 85k/200k — clear. - **admiral**: 99k/1M — OK - **gurney**: 117k/1M — OK (same reading, stable) - **watch**: 114k/200k — OK (57%, same 3 ticks — keeper gauge frozen; not at limit) - **captain**: 108k/1M — OK (same reading 2 ticks) - **flywheel**: 8th tick idle, no `.ctx`
[2026-06-24 20:44 | 03:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 20:44 | 03:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 20:44 | 03:44Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-24 20:44 | 03:44Z]  SAY  Tick done. Self at 90k/200k — clear. - **admiral**: 116k/1M — OK - **gurney**: 117k/1M — OK - **watch**: 114k/200k — OK (57%, frozen gauge 4 ticks — keeper may have stopped updating) - **captain**: 108k/1M — OK (same reading 3 ticks) - **flywheel**: 9th tick idle, no `.ctx`
[2026-06-24 21:14 | 04:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 21:14 | 04:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 21:14 | 04:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-24 21:15 | 04:15Z]  SAY  Tick done. Self at 95k/200k — clear (48%, approaching midpoint; still well under 250k limit). - **admiral**: 116k/1M — OK - **gurney**: 117k/1M — OK - **watch**: 114k/200k — OK (57%, same value 5 ticks — keeper gauge appears stuck) - **captain**: 108k/1M — OK (same value 4 ticks — same) - **flywheel**: 10th consecutive tick idle, no `.ctx`
[2026-06-24 21:44 | 04:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 21:44 | 04:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 21:44 | 04:44Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-24 21:45 | 04:45Z]  SAY  Tick done. Self at 100k/200k — 50%, approaching self-respawn threshold (125k = 62.5%). - **admiral**: 122k/1M — OK - **gurney**: 117k/1M — OK (same, keeper quiet) - **watch**: 114k/200k — OK (same 6 ticks; keeper gauge frozen but well under limit) - **captain**: 108k/1M — OK (same 5 ticks) - **flywheel**: 11th consecutive tick idle, no `.ctx`
[2026-06-24 22:14 | 05:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 22:14 | 05:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 22:14 | 05:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-24 22:15 | 05:15Z]  SAY  Tick done. Self at 105k/200k — 53%, still under 250k limit. - **admiral**: 58k/1M — OK (new session ID, keeper cycled it) - **gurney**: 117k/1M — OK - **watch**: 114k/200k — OK (same gauge 7 ticks, ts age 55s — borderline; still fresh) - **captain**: 108k/1M — OK (fresh ts) - **flywheel**: 12th tick idle, no `.ctx`
[2026-06-24 22:44 | 05:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 22:44 | 05:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 22:45 | 05:45Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-24 22:45 | 05:45Z]  SAY  Tick done. Self at 110k/200k — 55%, still under 250k limit. - **admiral**: 102k/1M — OK - **gurney**: 117k/1M — OK (same gauge, quiet) - **watch**: 114k/200k — OK (same 8 ticks — keeper gauge frozen but under limit) - **captain**: 108k/1M — OK (same gauge, keeper quiet) - **flywheel**: 13th tick idle, no `.ctx`
[2026-06-24 23:14 | 06:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 23:14 | 06:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 23:14 | 06:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-24 23:14 | 06:14Z]  SAY  Tick done. Self at 115k/200k — 58%, approaching the 250k window (125k = 62.5% of 200k). Will respawn next tick if over threshold. - **admiral**: 73k/1M — OK (new session, cycled again) - **gurney**: 141k/1M — OK (climbing, was 117k last tick) - **watch**: 114k/200k — OK (same 9 ticks, gauge frozen) - **captain**: 104k/1M — OK (new session, cycled) - **flywheel**: 14th tick idle, no `.ctx`
[2026-06-24 23:44 | 06:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-24 23:44 | 06:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-24 23:45 | 06:45Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-24 23:45 | 06:45Z]  SAY  Tick done. Self at 120k/200k — 60%, still under 250k limit. - **admiral**: 77k/1M — OK - **gurney**: 63k/1M — OK (new session, cycled) - **watch**: 114k/200k — OK (same 10 ticks — keeper gauge definitively frozen, but under limit) - **captain**: 179k/1M — OK (climbing, was 104k last tick) - **flywheel**: 15th tick idle, no `.ctx`
[2026-06-25 00:14 | 07:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 00:14 | 07:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 00:15 | 07:15Z]  SAY  Self at 125k/200k (63%) — still clear, but I'll trigger respawn at ~170k given my 200k window.
[2026-06-25 00:15 | 07:15Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 00:15 | 07:15Z]  SAY  Tick done. Self at 125k/200k. - **admiral**: 94k/1M — OK - **gurney**: 67k/1M — OK - **watch**: 114k/200k — OK (same 11 ticks, keeper gauge frozen) - **captain**: 179k/1M — OK (same as last tick) - **flywheel**: 16th tick idle, no `.ctx`
[2026-06-25 00:44 | 07:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 00:44 | 07:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 00:44 | 07:44Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 00:44 | 07:44Z]  SAY  Tick done. Self at 131k/200k — 66%, still clear. - **admiral**: 112k/1M — OK - **gurney**: 69k/1M — OK - **watch**: 114k/200k — OK (same 12 ticks; keeper gauge frozen) - **captain**: 201k/1M — OK (climbing steadily, was 179k) - **flywheel**: 17th tick idle, no `.ctx`
[2026-06-25 01:14 | 08:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 01:14 | 08:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 01:14 | 08:14Z]  SAY  Watch is gone from crew list and tmux — dead. Checking remaining.
[2026-06-25 01:14 | 08:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 01:15 | 08:15Z]  SAY  Tick done. Self at 137k/200k — 68%. - **admiral**: 123k/1M — OK - **gurney**: 87k/1M — OK - **captain**: 59k/1M — OK (new session, cycled) - **watch**: dropped from crew list + no tmux — dead, no action needed - **flywheel**: 18th tick idle, no `.ctx`
[2026-06-25 01:44 | 08:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 01:44 | 08:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 01:44 | 08:44Z]  SAY  Watch is back (restarted at 08:14 UTC). Checking all ctx files.
[2026-06-25 01:45 | 08:45Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 01:45 | 08:45Z]  SAY  Tick done. Self at 142k/200k — 71%. - **admiral**: 124k/1M — OK - **gurney**: 87k/1M — OK - **watch**: 103k/200k — OK (restarted by captain ~30min ago, fresh session) - **captain**: 66k/1M — OK - **flywheel**: 19th tick idle, no `.ctx`
[2026-06-25 02:14 | 09:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 02:14 | 09:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 02:14 | 09:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 02:15 | 09:15Z]  SAY  Tick done. Self at 146k/200k — 73%. - **admiral**: 139k/1M — OK - **gurney**: 87k/1M — OK (same, quiet) - **watch**: 117k/200k — OK (58%, fresh enough at 174s) - **captain**: 91k/1M — OK - **flywheel**: 20th tick idle, no `.ctx`
[2026-06-25 02:44 | 09:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 02:44 | 09:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 02:44 | 09:44Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 02:45 | 09:45Z]  SAY  Tick done. Self at 151k/200k — 76%. Approaching respawn threshold (~170k); ~4 ticks remaining at current rate. - **admiral**: 66k/1M — OK (new session, cycled) - **gurney**: 129k/1M — OK - **watch**: 139k/200k — OK (69%, ts age 283s — just fresh) - **captain**: 108k/1M — OK - **flywheel**: 21st tick idle, no `.ctx`
[2026-06-25 03:14 | 10:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 03:14 | 10:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 03:15 | 10:15Z]  SAY  Watch ctx is stale (14min). Checking pane.
[2026-06-25 03:15 | 10:15Z]  SAY  Watch pane shows idle Claude shell (not dead, not at cap). Self at 156k/200k — 78%, triggering respawn now per step-6 protocol.
[2026-06-25 03:15 | 10:15Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 03:15 | 10:15Z]  TOOL_RESULT(err)  Continue from where you left off.
[2026-06-25 03:15 | 10:15Z]  SAY  No response requested.
[2026-06-25 03:15 | 10:15Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 03:15 | 10:15Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 03:15 | 10:15Z]  SAY  Watch still stale (890s). Self at 162k/200k — triggering respawn this tick.
[2026-06-25 03:15 | 10:15Z]  TOOL_RESULT(err)  Continue from where you left off.
[2026-06-25 03:15 | 10:15Z]  SAY  No response requested.
[2026-06-25 03:15 | 10:15Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 03:15 | 10:15Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 03:17 | 10:17Z]  WAKE  This session is being continued from a previous conversation that ran out of context. The summary below covers the earlier portion of the conversation. Summary: 1. Primary Request and Intent: The
[2026-06-25 03:17 | 10:17Z]  SAY  All fresh, all under limits: - **admiral**: 66k/1M — OK - **gurney**: 150k/1M — OK - **watch**: 159k/200k — OK (fresh, under 300k) - **captain**: 114k/1M — OK - **flywheel**: live tmux, no .ctx file — skip (no keeper, not oversized)
[2026-06-25 03:17 | 10:17Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 03:17 | 10:17Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 03:17 | 10:17Z]  SAY  Tick 27 done. All 4 agents healthy, no restarts. Next tick at 03:48.
[2026-06-25 03:17 | 10:17Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 03:17 | 10:17Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 03:18 | 10:18Z]  SAY  All fresh, all under limits: - **admiral**: 66k/1M — OK - **gurney**: 150k/1M — OK - **watch**: 160k/200k — OK - **captain**: 116k/1M — OK - **flywheel**: live, no .ctx — skip
[2026-06-25 03:18 | 10:18Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 03:18 | 10:18Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 03:18 | 10:18Z]  SAY  Tick 28 done. All healthy, no restarts. Next tick at 04:18.
[2026-06-25 03:44 | 10:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 03:44 | 10:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 03:44 | 10:44Z]  SAY  Watch is stale (659s). Checking pane.
[2026-06-25 03:45 | 10:45Z]  SAY  Watch: stale + idle shell (no cap indicator), 164k at last read — OK. - **admiral**: 74k/1M — OK - **gurney**: 150k/1M — OK (frozen value, keeper batch behavior) - **watch**: 165k/200k — stale+idle pane, not at cap — OK - **captain**: 117k/1M — OK - **flywheel**: live, no .ctx — skip
[2026-06-25 03:45 | 10:45Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 03:45 | 10:45Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 03:45 | 10:45Z]  SAY  Tick 29 done. All healthy, no restarts. Next tick at 04:48.
[2026-06-25 04:14 | 11:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 04:14 | 11:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 04:14 | 11:14Z]  SAY  Watch stale (574s). Checking pane.
[2026-06-25 04:14 | 11:14Z]  SAY  Idle shell, no cap indicator. Gurney also dropped significantly (150k→60k = was restarted). All under limits: - **admiral**: 81k/1M — OK - **gurney**: 60k/1M — OK (fresh restart, context reset) - **watch**: 64k/200k — stale+idle pane, OK - **captain**: 134k/1M — OK - **flywheel**: live, no .ctx — skip
[2026-06-25 04:14 | 11:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 04:15 | 11:15Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 04:15 | 11:15Z]  SAY  Tick 30 done. All healthy, no restarts. Next tick at 05:18.
[2026-06-25 04:44 | 11:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 04:44 | 11:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 04:44 | 11:44Z]  SAY  Watch stale (903s). Checking pane.
[2026-06-25 04:44 | 11:44Z]  SAY  Idle shell, no cap indicator. All under limits: - **admiral**: 101k/1M — OK - **gurney**: 60k/1M — OK - **watch**: 76k/200k — stale+idle pane, OK - **captain**: 136k/1M — OK - **flywheel**: live, no .ctx — skip
[2026-06-25 04:44 | 11:44Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 04:45 | 11:45Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 04:45 | 11:45Z]  SAY  Tick 31 done. All healthy, no restarts. Next tick at 05:48.
[2026-06-25 05:14 | 12:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 05:14 | 12:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 05:14 | 12:14Z]  SAY  Watch stale (781s). Checking pane.
[2026-06-25 05:14 | 12:14Z]  SAY  Idle shell, no cap indicator. All under limits: - **admiral**: 101k/1M — OK - **gurney**: 60k/1M — OK - **watch**: 85k/200k — stale+idle pane, OK - **captain**: 140k/1M — OK - **flywheel**: live, no .ctx — skip
[2026-06-25 05:15 | 12:15Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 05:15 | 12:15Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 05:15 | 12:15Z]  SAY  Tick 32 done. All healthy, no restarts. Next tick at 06:18.
[2026-06-25 05:44 | 12:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 05:44 | 12:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 05:44 | 12:44Z]  SAY  All fresh, all under limits: - **admiral**: 107k/1M — OK - **gurney**: 100k/1M — OK - **watch**: 103k/200k — OK (fresh this tick, keeper resumed) - **captain**: 51k/1M — OK (reset, was restarted by its keeper) - **flywheel**: live, no .ctx — skip
[2026-06-25 05:44 | 12:44Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 05:45 | 12:45Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 05:45 | 12:45Z]  SAY  Tick 33 done. All healthy, no restarts. Next tick at 06:48.
[2026-06-25 06:14 | 13:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 06:14 | 13:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 06:14 | 13:14Z]  SAY  All fresh, all under limits: - **admiral**: 113k/1M — OK - **gurney**: 116k/1M — OK - **watch**: 132k/200k — OK (fresh, under cap) - **captain**: 64k/1M — OK - **flywheel**: live, no .ctx — skip
[2026-06-25 06:14 | 13:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 06:15 | 13:15Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 06:15 | 13:15Z]  SAY  Tick 34 done. All healthy, no restarts. Next tick at 07:18.
[2026-06-25 06:44 | 13:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 06:44 | 13:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 06:45 | 13:45Z]  SAY  All fresh (watch 229s < 300s), all under limits: - **admiral**: 132k/1M — OK - **gurney**: 116k/1M — OK - **watch**: 143k/200k — OK - **captain**: 72k/1M — OK - **flywheel**: live, no .ctx — skip
[2026-06-25 06:45 | 13:45Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 06:45 | 13:45Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 06:45 | 13:45Z]  SAY  Tick 35 done. All healthy, no restarts. Next tick at 07:48.
[2026-06-25 07:14 | 14:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 07:14 | 14:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 07:14 | 14:14Z]  SAY  Watch stale (725s). Checking pane.
[2026-06-25 07:14 | 14:14Z]  SAY  Idle shell, no cap indicator. All under limits: - **admiral**: 132k/1M — OK - **gurney**: 136k/1M — OK - **watch**: 155k/200k — stale+idle pane, OK - **captain**: 98k/1M — OK - **flywheel**: live, no .ctx — skip
[2026-06-25 07:14 | 14:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 07:15 | 14:15Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 07:15 | 14:15Z]  SAY  Tick 36 done. All healthy, no restarts. Next tick at 08:18.
[2026-06-25 07:44 | 14:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 07:44 | 14:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 07:45 | 14:45Z]  SAY  Watch stale (547s). Checking pane.
[2026-06-25 07:45 | 14:45Z]  SAY  Idle shell, no cap indicator. All under limits: - **admiral**: 140k/1M — OK - **gurney**: 136k/1M — OK - **watch**: 165k/200k — stale+idle pane, OK - **captain**: 104k/1M — OK - **flywheel**: live, no .ctx — skip
[2026-06-25 07:45 | 14:45Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 07:45 | 14:45Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 07:45 | 14:45Z]  SAY  Tick 37 done. All healthy, no restarts. Next tick at 08:48.
[2026-06-25 08:14 | 15:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 08:14 | 15:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 08:14 | 15:14Z]  SAY  All fresh, all under limits: - **admiral**: 55k/1M — OK (reset, keeper cycled it) - **gurney**: 137k/1M — OK - **watch**: 59k/200k — OK (fresh, keeper cycled it) - **captain**: 113k/1M — OK - **flywheel**: live, no .ctx — skip
[2026-06-25 08:14 | 15:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 08:15 | 15:15Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 08:15 | 15:15Z]  SAY  Tick 38 done. All healthy, no restarts. Next tick at 09:18.
[2026-06-25 08:44 | 15:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 08:44 | 15:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 08:44 | 15:44Z]  SAY  All fresh (watch 229s < 300s), all under limits: - **admiral**: 88k/1M — OK - **gurney**: 57k/1M — OK - **watch**: 71k/200k — OK - **captain**: 120k/1M — OK - **flywheel**: live, no .ctx — skip
[2026-06-25 08:44 | 15:44Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 08:45 | 15:45Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 08:45 | 15:45Z]  SAY  Tick 39 done. All healthy, no restarts. Next tick at 09:48.
[2026-06-25 09:14 | 16:14Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 09:14 | 16:14Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 09:14 | 16:14Z]  SAY  All fresh, all under limits: - **admiral**: 89k/1M — OK - **gurney**: 90k/1M — OK - **watch**: 82k/200k — OK - **captain**: 139k/1M — OK - **flywheel**: live, no .ctx — skip
[2026-06-25 09:14 | 16:14Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 09:15 | 16:15Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 09:15 | 16:15Z]  SAY  Tick 40 done. All healthy, no restarts. Next tick at 10:18.
[2026-06-25 09:44 | 16:44Z]  WAKE  <command-message>loop</command-message> <command-name>/loop</command-name> <command-args>30m Context-watchdog tick — keep every live CREW/flywheel session under 300k tokens. Do ONLY this, terse (one l
[2026-06-25 09:44 | 16:44Z]  TOOL_RESULT(err)  # /loop — schedule a recurring or self-paced prompt Parse the input below into `[interval] <prompt…>` and schedule it. ## Parsing (in priority order) 1. **Leading token**: if the first whitespace-delimited token matches `^\d+[smhd]$` (e.g. `5m`, `2h`), that's the interval; the rest is the prompt. 2. **Trailing "every" clause**: otherwise, if the input ends with `every <N><unit>` or `every <N> <unit-word>` (e.g. `every 20m`, `every 5 minutes`, `every 2 hours`), extract that as the interval and strip it from the prompt. Only match when what follows "every" is a time expression — `check every PR` has no interval. 3. **No interval**: otherwise, the entire input is the prompt and you'll self-pace dynamically (see "Dynamic mode" below). If the resulting prompt is empty, show usage `/loop [interv … [+8351 chars]
[2026-06-25 09:44 | 16:44Z]  SAY  All fresh, all under limits: - **admiral**: 112k/1M — OK - **gurney**: 90k/1M — OK - **watch**: 88k/200k — OK - **captain**: 170k/1M — OK - **flywheel**: live, no .ctx — skip
[2026-06-25 09:44 | 16:44Z]  COMMS  from=ctx-watchdog to=operator topic=watchdog  ✓bus
[2026-06-25 09:45 | 16:45Z]  WAKEUP  +1800s — 30-min watchdog cadence; all agents healthy this tick
[2026-06-25 09:45 | 16:45Z]  SAY  Tick 41 done. All healthy, no restarts. Next tick at 10:48.
