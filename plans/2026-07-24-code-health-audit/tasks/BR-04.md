# BR-04 — Thin beadRunOne to a phase coordinator

## Dispatch metadata

- Group / priority: run architecture / P0
- Execution profile: `sol_xhigh`
- Reviewer profile: `sol_xhigh`
- Depends on: `BR-03`, `JR-01`, `JR-03`, `ARCH-GATE`
- Work type: final per-run spine decomposition

## Objective

Make `beadRunOne` a thin, typed phase coordinator over resolve, prepare,
execute, finalize, and terminal owners. Remove remaining policy/effect blocks
from the function. Relocation is optional and is not itself acceptance.

## Exclusive lease

`beadRunOne`, exact extracted owners/call sites, and focused fidelity tests.
Sole `dispatch_spine` writer; no shared baseline file. Any package move requires
the existing operator/quiescence gate.

## Required work

1. Replace remaining inline branches with phase calls and typed results.
2. Remove compatibility construction and direct `workLoopDeps` reach.
3. Prove cleanup on every early return and panic boundary.
4. Meet the `ARCH-01` span/complexity/import-budget target.
5. Run the compile probe before any optional relocation.

## Acceptance

- `beadRunOne` expresses orchestration order, not subsystem policy.
- No hidden durable transition or process cleanup remains inline.
- The production graph constructs every dependency before entry.
- Complexity/span fall below the reviewed architectural target.
- A fresh Sol reviewer approves behavior fidelity and ownership.

## Verification

Full per-run fidelity/fault/race/stress/real-process matrix, architecture gate,
production composition proof, lint/UBS, and `make check-short`.

## Escalate when

Stop before package relocation without explicit operator approval and a fresh
fleet-quiescence check. A moved giant function is not completion.

## Return

Use the directory return contract. **COMMIT EXPLICITLY.**
