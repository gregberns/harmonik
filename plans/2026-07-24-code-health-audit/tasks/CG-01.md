# CG-01 — Assemble one immutable production run graph

## Dispatch metadata

- Group / priority: run architecture / P0
- Execution profile: `sol_xhigh`
- Reviewer profile: `sol_xhigh`
- Depends on: `ARCH-01`, `WL-04`, `BR-04`, `RL-03`, `DOT-03`, `CQ-06`, `JR-04`, `PI-F1`, `PI-F2S`, `PI-F2D`, `PI-R2`
- Work type: composition-root replacement

## Objective

Add constructor-complete typed graph builders beside the current temporal
`workLoopDeps` assembly. Construct outer-loop services, per-run factory, mode
executors, and terminal ownership in tests without switching production yet.
This is the typed strangler foundation, not a flag-day migration.

## Exclusive lease

Typed constructor files adjacent to current composition, focused constructor
tests, and only the minimal construction sites needed to prove coexistence.
No production cutover and no shared baseline file.

## Required work

1. Add complete constructors for each `ARCH-01` owner.
2. Split daemon-wide services from per-run immutable facts.
3. Reject incomplete construction with types/constructors.
4. Prove old and new graphs can coexist while callers migrate.
5. Enforce dependency direction and import budgets.

## Acceptance

- The compiler or constructor types prevent a runnable partial graph.
- No production behavior changes in this task.
- Controlled tests can assemble the complete target graph without late
  population.
- No mode imports daemon or reaches a generic service locator.

## Verification

Constructor-negative and coexistence tests, depguard and architecture gates,
daemon/runloop suites, lint/UBS, and `make check-fast`.

## Escalate when

Stop if complete constructors require a flag day or a generic service bag.

## Return

Use the directory return contract. **COMMIT EXPLICITLY.**
