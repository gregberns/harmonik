# CQ-01 — Enforce the queue admission transaction

## Dispatch metadata

- Group / priority: core queue / P0
- Execution profile: `terra_high`
- Reviewer profile: `sol_xhigh`
- Depends on: `CQ-02I`
- Work type: bounded contract-conformance implementation

## Objective

Make supported CLI/socket submission and append obey the existing admission
contract: validate completely, persist exactly once before install/wake/events,
and leave no runnable in-memory item on rejection or durable-write failure.

## Evidence to verify first

Use `tasks/evidence/CQ-00.yaml`, `specs/queue-model.md`
QM-027/QM-050/QM-063/EM-065, and:

- `internal/queue/rpc.go` `HandleQueueSubmit`, `HandlerAdapter.HandleQueueSubmit`,
  `HandleQueueAppend`, `HandlerAdapter.HandleQueueAppend`, `appendUnderLock`,
  and the unlocked fallback;
- `internal/queue/cli` submit/append;
- `internal/daemon/bootsocket.go` production adapter construction.

## Exclusive lease

- `internal/queue/transaction_store.go` for queue-owned transaction DTOs and
  the narrow admission transaction port
- `internal/queuewiring/store.go` to replace its local `Snapshot`,
  `TransactionRequest`, and `TransactionResult` declarations with aliases to
  the queue-owned DTOs and to add a detached all-name transaction validation
  view evaluated while `queueMu` is held; do not change persistence or
  installation ordering
- `internal/queuewiring/store_transaction_test.go` for the all-name validation
  view, stale-view, and distinct-name concurrency proofs
- `internal/queue/rpc.go` `HandleQueueSubmit`,
  `HandlerAdapter.HandleQueueSubmit`, `HandleQueueAppend`,
  `HandlerAdapter.HandleQueueAppend`, `appendUnderLock`, the unlocked fallback,
  and focused RPC tests
- compatibility migrations in
  `internal/scenario/queue_setqueue_wiring_test.go`,
  `internal/scenario/single_active_per_name_test.go`, and
  `internal/queuewiring/store_append_lostupdate_test.go`
- exact submit/append CLI composition tests, if needed
- no `internal/daemon/workloop.go`, persistence implementation, or queue state
  kernel edits

## Required work

1. Preserve package-level `HandleQueueSubmit` and `HandleQueueAppend` as
   validation/build compatibility helpers; they are not production transaction
   boundaries and must not gain a store parameter or claim atomic admission.
   Add failing-first boundary tests through both helpers and both production
   adapter entry points. Accepted, validation-rejected, persist-failed, and
   concurrent behavior is owned by the adapter boundary.
2. Exercise the production `HandlerAdapter` with its real mutation-lock shape.
   The adapter must depend on the queue-owned admission transaction port; the
   daemon's existing `*queuewiring.QueueStore` must satisfy that port directly.
   Preserve existing queuewiring callers with type aliases so this seam adds no
   reverse import and requires no reflection or composition-root callback.
   `NewHandlerAdapter` may retain its constructor shape, but submit and append
   must fail closed when its `QueueSetter` does not also implement the
   transaction port. Migrate success-path fixtures to a transaction-capable
   store; keep non-admission compatibility tests only where their behavior is
   unaffected.
3. Remove or fail closed on any production-reachable unlocked mutation fallback;
   retain a test-only compatibility seam only if the reviewer can prove it is
   absent from production wiring.
4. Verify event/wake ordering from observable effects, not internal call counts.
5. Extend the generic transaction request with an optional pre-mutation
   validator that receives a deep-cloned snapshot of the complete normalized
   QueueStore named set while `queueMu` is held. Admission uses this view for
   the EM-064/EM-065 duplicate oracle. Existing non-admission transactions may
   omit it. The callback must never receive live pointers and must run before
   persistence; a rejection changes neither memory nor disk.

## Acceptance

- Rejection and persistence failure leave disk and caller-visible memory
  unchanged and emit no success event.
- Successful submit/append persists before install, wake, or success events.
- Concurrent same-name admission has one winner and no lost accepted request.
- Distinct free normalized names may submit concurrently; duplicate pre-screen
  covers the complete immutable QueueStore named set at the serialized
  transaction point. Two concurrent distinct-name submissions carrying the
  same bead cannot both commit.
- Production wiring cannot select the unlocked fallback.

Accepted intermediate: submit/append use the generic transaction while
receipt/status remains disabled; release `internal/queue/rpc.go` before
`CQ-RECEIPT`. Rollback all four named submit/append entry points,
`appendUnderLock`, the unlocked-fallback closure, and their focused tests
together; do not leave append on a different admission path from submit.

## Verification

Focused queue RPC tests, race test for concurrent submit/append, scoped vet/lint,
UBS, independent production-call-site review, and post-commit `make check-fast`.

## Escalate when

Stop if satisfying the tests requires a new persisted generation/CAS contract,
changing queue file format, or editing the dispatch stamp; those belong to
`CQ-02` or `CQ-03`.

## Return

Use the directory return contract. **COMMIT EXPLICITLY.**
