# CQ-05 — Close gaps in existing queue decision kernels

## Dispatch metadata

- Group / priority: core queue / P1
- Execution profile: `pi_ralph`
- Reviewer profile: `terra_high`
- Depends on: `CQ-01`, `CQ-03`
- Work type: evidence/closure review, not a greenfield refactor

## Objective

Review the already-pure queue kernels (`internal/queue/state.go` and
`internal/orchestrator/select.go`) against the corrected admission/reservation
contract. Add only missing table/property cases or a proven small pure fix.

## Exclusive lease

- `internal/queue/state.go` and its focused tests
- `internal/orchestrator/select.go` and its focused tests
- no daemon adapter/composition, persistence, or run files

## Required work

1. Build an exhaustive state/input table from current enums and contract.
2. Map existing tests; do not duplicate covered cases.
3. Add properties: terminal absorption, deterministic eligibility, no second
   owner, and reason stability where reasons exist.
4. Make a production change only for a failing table case with an unambiguous
   expected result.

## Acceptance

- Every current queue/group/item state is classified.
- Existing pure kernels are reused rather than replaced.
- Any code change is small, deterministic, and covered by a failing-first case.
- If no gap exists, close with reviewed evidence and no production diff.

## Verification

Focused pure package tests, property repetitions, lint/UBS for changed files,
and independent review.

## Escalate when

Stop if the expected result depends on I/O ordering or a new policy decision.

## Return

Return the coverage table and any bounded commit. **COMMIT EXPLICITLY** if code
or tracked evidence changes.

