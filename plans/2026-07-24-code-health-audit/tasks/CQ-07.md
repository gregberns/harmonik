# CQ-07 — Prove one queue item survives restart safely

## Dispatch metadata

- Group / priority: core queue / P0
- Execution profile: `terra_high`
- Reviewer profile: `sol_xhigh`
- Depends on: `CQ-03`, `CQ-04`, `CQ-06`
- Work type: scenario proof

## Objective

Extend the existing hermetic daemon scenarios to prove supported submission,
durable reservation, controlled stop/restart, and exactly-one claim/recovery
classification. Do not invent a second scenario harness.

## Evidence to verify first

Re-run and inspect:

- `scenario_queue_submit_dispatch_hksk00a_test.go`
- `scenario_restart_recovery_ivzsl_test.go`
- `scenario_dispatch_tracker_orphan_hkiwu8a_test.go`

Identify the smallest missing restart cut rather than rebuilding their fixtures.

## Exclusive lease

- the existing daemon scenario fixtures/tests named above
- one narrowly shared scenario helper if required
- no production semantics unless a separate task is created

## Required work

1. Add controlled cuts after admission, reservation, claim, and before terminal
   persistence where current scenarios lack proof.
2. Assert durable queue, Bead, Run, and event facts after restart.
3. Use deterministic barriers/channels, never timing sleeps as the oracle.
4. Count launch/claim attempts and require one runnable owner.

## Acceptance

- A supported item is either executed once or classified safely after restart.
- No cut yields duplicate launch, leaked claim, or silent pending/dispatched
  limbo.
- The test runs without the primary daemon or real paid model.
- Existing happy-path behavior remains green.

## Verification

Focused scenario tests, race run, repeat count, fault cases, vet/lint/UBS for
changed helpers, review, and `make check-fast`.

## Escalate when

Stop and file a production task if the scenario exposes a new semantic defect;
do not patch it inside the test card.

## Return

Use the directory return contract. **COMMIT EXPLICITLY.**
