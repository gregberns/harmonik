# JR-02 — Close gaps in existing terminal decision machines

## Dispatch metadata

- Group / priority: job/run / P1
- Execution profile: `pi_ralph`
- Reviewer profile: `terra_high`
- Depends on: `JR-00`
- Work type: evidence/closure review

## Objective

Map terminal outcomes to the existing `internal/runexec` Run/Dispatch machines
and `handler.MapWaitReturnToTerminalEvent`. Add only missing exhaustive tests or
a proven small pure fix.

## Exclusive lease

`internal/runexec` and the pure handler outcome mapper/tests only. No workloop,
queue, ledger, persistence, or process lifecycle files.

## Acceptance

All current outcomes are classified; terminal states absorb later input; unknown
input fails loudly; no redundant classifier is introduced. Close with reviewed
evidence and no code if coverage is already complete.

## Verification

Pure table/property tests, lint/UBS for changed files, independent review.

## Escalate when

Stop when the expected result depends on side-effect ordering or retry policy.

## Return

Commit accepted evidence or a bounded pure fix explicitly.

