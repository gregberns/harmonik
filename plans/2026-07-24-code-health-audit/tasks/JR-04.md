# JR-04 — Reconcile orphaned Run and claim state to a fixed point

## Dispatch metadata

- Group / priority: job/run / P0
- Execution profile: `sol_xhigh`
- Reviewer profile: `sol_xhigh`
- Depends on: `CQ-04`, `JR-03`
- Work type: bounded recovery adapter

## Objective

Apply reviewed queue recovery decisions to orphaned Run/claim evidence,
including production live-session adoption, without duplicate execution or
leaked ownership. Workloop caller migration belongs to `WL-REC-01`.

## Exclusive lease

- `internal/daemon/runinflightreconcile_hkr73qr.go`
  `reconcileOrphanedRunsOnResume` and focused tests
- `internal/daemon/run_session_adoption.go` `adoptLiveRunSession` and tests
- startup queue classifier belongs to `CQ-04`

## Acceptance

Repeated recovery is idempotent; PID/session ambiguity fails closed; adopted
and retried runs cannot both own the item; all writes/events are ordered and
observable.
The recovery/adoption owner exposes one typed decision/effect boundary ready
for `WL-REC-01`.
Queue receipt decisions are consumed without JSONL completion inference.
Accepted intermediate: typed adoption exists while workloop call sites remain
inline until `WL-REC-01`. Roll back only these two production files, boundary,
and tests.

## Verification

Focused recovery/fault/race tests, restart repeat, lint/UBS, review, check-fast.

## Escalate when

Stop if adoption needs a changed process-provenance contract.

## Return

Use the directory return contract. **COMMIT EXPLICITLY.**
