# PI-00 — Re-validate the local Pi lifecycle map

## Dispatch metadata

- Group / priority: Pi lifecycle / P0
- Execution profile: `sol_xhigh`
- Reviewer profile: `terra_high`
- Depends on: none
- Work type: security/lifecycle characterization and task-card validation

## Objective

Produce `tasks/evidence/PI-00.yaml` and independently validate that the Pi task
decomposition matches current production code. The prior index's “complete”
claim had no review record and is not accepted as ground truth.

## Evidence to verify first

Trace `internal/harness/pi`, `internal/handler/session.go`, the single,
review-loop, and DOT call sites, queue harness selection, and real/twin Pi
tests. Confirm:

- secret/key-file and generated `models.json` behavior;
- disk-auth guard path and actual configured Pi directory;
- session-ID capture, `agent_end`, readiness, process exit, kill, and wait;
- initial versus resume launch directory/config;
- ProcessExit finalization and event ordering;
- queue default and explicit label routing;
- local versus remote refusal.

## Exclusive lease

- Read any source/test/spec and current Pi installation metadata needed for
  format confirmation.
- Write only `tasks/evidence/PI-00.yaml`.
- Do not edit production code, tests, credentials, the index, or Pi's live
  configuration.

## Required work

1. Record current source symbols and tests for each lifecycle stage.
2. Confirm claims with safe fixtures or read-only format inspection; never print
   credential values.
3. Review the proposed `PI-SPEC-01`, `PI-A1`, `PI-A2A`, `PI-A2B`, `PI-L1A`,
   `PI-L1B`, `PI-L1C`, and `PI-F0` boundaries and list exact lease corrections.
4. Separate spawn proof, captured identity, genuine readiness, completion, and
   process reaping.
5. Separate local-core blockers from remote and rate-limit follow-ons.

## Acceptance

- The artifact contains no secret values or secret-bearing command output.
- Every ready Pi task has a verified defect statement, bounded lease, and
  failing-first test seam.
- Local `PI-E1` does not depend on functional SSH or rate-limit policy.
- A different frontier agent reviews the decomposition before Pi implementation
  tasks become ready.

## Verification

- Parse the evidence YAML.
- Run only focused non-secret Pi/handler tests needed to challenge a claim.
- Reviewer checks security boundaries and all three production workflow modes.

## Escalate when

Stop if confirming a fact would read or expose a credential value, mutate the
operator's Pi state, or require the primary daemon.

## Return

Return the evidence artifact plus required index/card corrections. Commit the
accepted evidence explicitly.
