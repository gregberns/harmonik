# RL-01 — Reconcile the existing reviewloop decomposition

## Dispatch metadata

- Group / priority: run architecture / P0
- Execution profile: `sol_xhigh`
- Reviewer profile: `sol_xhigh`
- Depends on: `ARCH-00`
- Work type: branch/kerf reconciliation and task design

## Objective

Produce `tasks/evidence/RL-01.yaml`: reconcile the
`reviewloop-decoupling` branch/kerf work with the current integration tree.
Determine the factual status of R0–R12, reviewcycle, continuity, registrations,
subscriptions, and the blocked L8 move. Do not design implementation cards or
choose the future architecture.

## Exclusive lease

Only `tasks/evidence/RL-01.yaml`. Git, kerf, task, and source inspection are
read-only.

## Required work

1. Verify ancestry, diffs, tests, reviews, and production callers for each
   purportedly landed reviewloop atom.
2. Classify reusable kernels versus unused shadow architecture.
3. Inventory daemon-private dependencies blocking package ownership.
4. Record the 18 private dependency/29-site claim against current symbols.
5. Record whether L8 is mechanically resumable, blocked, or superseded by
   later reviewed work; `ARCH-01` makes the architecture decision.

## Acceptance

- Every old task has one disposition with live evidence.
- No competing reviewcycle or continuity implementation is proposed.
- `ARCH-01` can consume the evidence without trusting stale branch claims.
- No task/index/lease authority is exercised by this worker.

## Verification

Git ancestry/diff checks, focused existing tests, kerf reconciliation review,
and zero production diff.

## Escalate when

Stop if divergent branches implement incompatible normative behavior or require
history rewriting to reconcile.

## Return

Use the directory evidence return contract. **COMMIT EXPLICITLY.**
